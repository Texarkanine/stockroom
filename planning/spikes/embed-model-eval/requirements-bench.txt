# Deps for running the embedding-model benchmark on any machine (incl. macOS).
# torch is pulled in transitively by sentence-transformers; on Apple Silicon the
# default wheel includes MPS support, on Linux/CUDA install the matching torch
# first if you want GPU. Install into any interpreter / venv:
#
#   python3 -m pip install -r requirements-bench.txt
#
sentence-transformers>=3,<6
duckdb>=1.1
numpy>=1.24
