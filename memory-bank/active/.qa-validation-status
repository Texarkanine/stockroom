PASS

QA review of `warehouse-schema-docs` (Level 3) after the column-meaning rework.

## Verified

- The generated ERD visibly labels the empty-primary-key `session_token_usage` entity as a view and retains its logical relationship to `sessions`.
- `sr-query` keeps concise query-critical meanings that the ERD cannot convey: workspace identifiers and rollups, entrypoint discovery, omitted thinking, and tool-input-only storage.
- The generator derives entities and attributes from the head golden, derives edges from migration annotations, rejects incomplete or invalid relationship coverage, and compares the committed artifact without writing during `--check`.
- The Advanced docs path symlinks to the skill SSOT; CI, Make, and contributor documentation all use the same regeneration/check workflow.
- `make schema-docs-check`, the 18 schema-document tests, 4 skill-hygiene tests, and ruff check/format validation for `scripts/` pass.

## Advisory

- Full-suite results from the immediately preceding build remain applicable: all engine tests passed under Node 22 except the documented pre-existing `/tmp` identity failure; this QA rerun exercised the changed feature boundary directly.
