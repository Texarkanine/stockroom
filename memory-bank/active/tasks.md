# Tasks: Phase 2 · Milestone 3 — `sr-search` (sub-run m3)

> Stub — the Level 3 plan phase will populate the task breakdown, TDD plan, and step ordering.

The blended keyword + semantic search entrypoint: route a question to SQL / vector / blend,
run keyword and semantic search, merge and rank the result sets, and apply a context-aware
read-time truncation level.
