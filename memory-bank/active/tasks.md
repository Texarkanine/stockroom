# Tasks: `sr-query` skill (p2-embeddings-search m4)

Author the `sr-query` SKILL.md wrapping `python -m stockroom.query`: ergonomic, safe LLM guidance for read-only SQL over the warehouse (when/how to use it, `--format` / `--detail`, guardrails against context blowout & wasted tool calls) + any helper `scripts/`.

_Checklist to be populated by the Level 2 PLAN phase._
