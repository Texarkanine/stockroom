# Current Task: Load section information architecture

**Complexity:** Level 2

* Task ID: load-section-ia
* Type: simple enhancement (docs)

Rework: turn `docs/user-guide/load/index.md` into a section router, relocate the orphaned Harness-Specific Notes block (splitting generic ingest content back into `basic.md` and per-harness reference into a new `sources.md`), add `load/.pages`, and repair the inbound link fallout from the `ingest/` → `load/` rename. No engine or CLI changes.

*Plan to be populated by the PLAN phase.*
