PASS (with advisory)

Task: p2-embeddings-search — sub-run m1 (Embedding pipeline)
Complexity: Level 3
Phase: L3 Preflight (implementation-plan validation)
Date: 2026-06-28

Validated the tasks.md implementation plan against codebase reality. PASS.

Checks:
  - Prerequisites: plan complete; 3 creative docs present (vss-provisioning, owner-grain, incremental).
  - TDD plan encoding (blocking): initially under-specified (cycle stated only in the preamble).
    FIXED in-phase — every implementation step now has explicit (a) test-first / (b) implement
    substeps; an implementer cannot follow it code-first. Now PASS.
  - Convention compliance: PASS. `stockroom.embed` single runnable module mirrors `stockroom.query`;
    `0003_<slug>.sql` + `test_schema_0003.py` + `0003_snapshot.json` mirror the 0001/0002 precedent;
    `ensure_vss` lives in the chokepoint (warehouse.py) that already owns per-connection setup.
  - Dependency impact: PASS. Migration head 2->3 ripple enumerated (test_migrate_runner,
    test_warehouse_open, migrated_con fixture, new test_schema_0003 + snapshot); discovery test is robust.
  - Conflict detection: PASS. No existing vss/embedding code; warehouse.open() public signature unchanged
    (adds vss loading behavior only); no broken contract.
  - Completeness: PASS. All milestone requirements (VSS/HNSW 0003, all-MiniLM 384, chunk+mean-pool,
    GPU-or-CPU, FLOAT[384] via chokepoint, incremental) map to concrete steps with planned tests.

Advisory findings (non-blocking; for the build):
  - [medium] ensure_vss runs LOAD + SET persistence on read-only connections (m2 readers need vss).
    LOAD/SET are session-level, expected to succeed on RO — VERIFY in step 3 with a reader test;
    if SET is rejected on RO, scope SET to writer/migrator and have readers only LOAD.
  - [low] 0003 snapshot: columns/PKs are unchanged from 0002, so the cumulative golden additionally
    captures an index section (HNSW index name/table/column/metric) so 0003 is a meaningfully locked artifact.
  - [low] Applied radical-innovation: embed CLI gains a `--full` flag mirroring ingest's reset.

No FAIL conditions. Cleared to the manual /niko-build gate.
