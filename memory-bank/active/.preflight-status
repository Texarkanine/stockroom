task_id: cursor-vscdb-backfill
complexity_level: 3
date: 2026-07-25
status: PASS
result: PASS with amendments applied to memory-bank/active/tasks.md

TDD plan encoding: PASS (after amendment)
  Steps 1-7 now carry ordered, blocking substeps: stub tests -> stub interface ->
  write and fail tests -> implement. Step 8 is documentation-only.

Findings remediated in-plan:
  1. TDD plan encoding (blocking) - steps carried a "TDD cycle" label over
     implementation-only substeps; restructured into explicit test-first ordering.
  2. Conflict - tests/test_config.py::test_settings_has_no_state_vscdb_field is a
     negative ratchet from the aborted enhance-cursor-tokens task that asserts the
     opposite of step 1; its deletion is now an explicit, justified plan substep.
  3. Convention - CLI main() moved from backfill/__init__.py to
     backfill/__main__.py, matching stockroom.ingest and stockroom.dashboard.
  4. Dependency impact - sessions.source_mtime was unspecified; it seeds
     messages.first_seen_at and the dashboard COALESCE(started_at, source_mtime)
     activity clock. Resolved as D8 (operator-decided): source_mtime stays NULL
     because a shared store's file mtime is not any composer's activity time,
     and the writer gains a utc_now() fallback so first_seen_at is still honest.
  5. Convention - docs/advanced/cli.md dropped from scope; its subcommand table is
     scoped to read surfaces and omits ingest/embed.
  6. Reuse - ISO timestamp parsing pinned to the ingest.claude._parse_ts precedent;
     stockroom.timestamps has no ISO helper and does not need one.

Advisory accepted and folded into the plan:
  7. D7 - `--force` re-parse scoped by source_path to rows this source authored.

Implementation plan is now 9 steps (writer D8 inserted as step 5).

Gate: /niko-build is unblocked.
