PASS WITH ADVISORY

Re-preflight of `warehouse-schema-docs` (Level 3) after the plan folded the CI ruff-lane fix into unit 6 (Engine CI step). All checks pass; the prior `FAIL (fixable)` finding is resolved.

## Verified

- **Prior finding resolved.** `tasks.md` unit 6 item 2 now extends the engine-job `Lint` / `Format check` steps in `.github/workflows/ci.yaml` to `ruff check . ../scripts` / `ruff format --check . ../scripts`, alongside the root-`Makefile` extension in unit 5. Confirmed against the current `ci.yaml` and `Makefile`: engine-job Lint/Format currently run bare `ruff check` / `ruff format --check` with cwd `skills/sr-search` and would otherwise miss `scripts/gen_warehouse_schema.py`.
- **TDD Plan Encoding — PASS.** Executable units 1–5 each place stub tests, then stub interface, then red tests, then green code, in that order. Prose/policy units 6–10 correctly carry no test steps, and the plan's own Integration Tests section already forbids change-detectors on `SKILL.md` phrasing, `ci.yaml` text, `.pages` nav labels, and Architecture wording — none were found.
- **Convention Compliance — PASS.** `scripts/gen_warehouse_schema.py` lands in the pre-existing repo-root `scripts/` dir (currently only `localdev.sh`); `skills/sr-query/references/warehouse-schema.md` follows the PPL-S `skills/**/references/**` override and the `docs/advanced/cookbook/` symlink precedent (verified against `test_query_cookbook.py` and `docs/advanced/.pages`); test file lands in the standard `skills/sr-search/tests/` location.
- **Dependency Impact — PASS.** Verified only `migrations/0001_initial_schema.sql` and `migrations/0007_session_token_usage.sql` contain `CREATE TABLE`/`CREATE VIEW` (grepped the full `migrations/` tree) — the plan's two-migration annotation scope is complete; no other migration needs a `@rel`/`@rel-none` line. Verified the head `0008_snapshot.json` PKs match the pinned `@rel` column lists (`messages(harness, session_id)`, `tool_calls(harness, session_id, message_id)`) and that `session_token_usage` has `"primary_key": []`, confirming the view heuristic.
- **Conflict Detection — PASS.** No existing ERD/schema diagram in `docs/architecture/warehouse.md` or `docs/advanced/duckdb.md` to fork from; no prior Python file under `scripts/`; no DDL or runtime schema change (comments only), so no public contract is broken.
- **Completeness Precheck — PASS.** All 7 project-brief requirements and 6 acceptance criteria map to concrete steps: visual-in-skill/docs (1, 3, 4), DDL-authoritative + lockstep (1, 3), CI drift gate (5, 6), local-runnable generator (1, 9), no-dummy-DuckDB (pinned pipeline), no-local-install (stdlib-only, confirmed no imports beyond `json`/`pathlib`/`re` are planned).

## Advisory

1. Unit 3 ("Commit SSOT + repo lockstep") carries the field `No tests: n/a — this step is executable`, copied from the prose/policy units' template, even though the unit correctly lists stub-test/red/green steps. Cosmetic only — the actual TDD ordering is correct — but worth a one-line fix next time `tasks.md` is touched so the field reads `Creative ref: none` like units 1, 2, 4, and 5.
2. `../scripts` has no ancestor `[tool.ruff]` config (root `pyproject.toml` is docs-only), so `ruff check ../scripts` will lint under ruff's built-in defaults rather than the engine's `target-version = "py311"` pin (falls back to the root stub's `requires-python = ">=3.10"`). Immaterial for stdlib-only code, but if the generator ever grows py311-only syntax, add a minimal `[tool.ruff]` block to root `pyproject.toml` to keep the two lanes in lockstep.
3. **Radical innovation (not applied):** extend the same comment-driven-generation idea one step further — add a sibling `-- @doc <column>: <description>` comment convention beside `@rel`/`@rel-none`, and have `render_markdown` emit a generated per-entity column-description table in the ERD doc. This would let `sr-query/SKILL.md`'s remaining hand-maintained prose shrink to pure judgement (when to use `--detail compact` vs `snippet`, join discipline) with zero schema *facts* left un-generated — closing the last drift vector this plan still leaves as prose. Concrete enough to evaluate against the added generated-doc surface; do not adopt without operator sign-off, since it changes the migration-comment contract again.
