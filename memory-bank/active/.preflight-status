FAIL (fixable)

Re-preflight of `warehouse-schema-docs` (Level 3) after the plan adopted migration `@rel` declarations.

## Findings

1. **Fixable — CI does not lint or format-check the new generator.** Step 5 correctly adds `../scripts` to the root Makefile's `lint`, `format`, and `format-check` targets. However, `.github/workflows/ci.yaml` does not invoke those targets: its engine job runs `uv run --no-sync --no-config ruff check` and `ruff format --check` from `skills/sr-search`. Those commands omit the repo-root `scripts/gen_warehouse_schema.py`. Update the CI lint and format commands to include `../scripts`, or replace them with the equivalent root Make targets. The planned `make schema-docs-check` verifies generated-content drift but cannot catch Python style or static-analysis failures in the generator.

## Verified

- **TDD Plan Encoding — PASS.** Executable units 1–5 each place stub tests and interfaces, then red tests, before production code. The workflow, skill, docs, contributor guide, and memory-bank text are correctly classified as prose/policy; no change-detector tests are scheduled.
- **Convention, dependency, conflict, and completeness checks — PASS apart from the CI lint lane above.** The plan uses the established cookbook symlink contract, derives boxes and columns from the head golden snapshot, keeps migration SQL authoritative, retains Architecture as routing rather than a DDL dump, and maps all project-brief requirements to concrete steps. The declared `@rel` / `@rel-none` grammar covers all six head entities without changing DuckDB DDL or runtime behavior.
- **No collision found.** The proposed generator and generated skill reference do not exist yet; the target directories and PPL-S reference override are appropriate.

## Advisory

Have `parse_rels_dir()` retain the migration filename for each parsed relationship and emit a short generated relationship-source appendix. This would let a reader trace a diagram edge directly to the migration comment that declares it, without adding another source of truth. Do not add it unless the operator accepts the additional generated-document scope.
