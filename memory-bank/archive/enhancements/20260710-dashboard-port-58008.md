---
task_id: dashboard-port-58008
complexity_level: 2
date: 2026-07-10
status: completed
---

# TASK ARCHIVE: dashboard-port-58008

## SUMMARY

Changed the local dashboard default port from `6767` to `58008` via scoped find/replace across engine, tests, skill, docs, and `techContext.md`. Clean break: no migration, no dual-port logic, no kill of orphaned 6767 listeners. `make ci` green (511 passed, 3 skipped).

## REQUIREMENTS

1. Change the dashboard default port from `6767` to `58008` everywhere it appears in the working tree except `memory-bank/archive/`.
2. Prefer a dead-simple find/replace — no behavioral redesign.
3. No migration path, identity cleanup, or dual-port logic.
4. Default launch (no `--port`) binds and advertises `58008`; tests/skills/non-archive docs updated; suite still passes.

## IMPLEMENTATION

Path-scoped find/replace (`6767` → `58008`), deliberately excluding `memory-bank/archive/`, `memory-bank/active/` narrative, and `skills/sr-search/uv.lock` (hash substrings that contain `6767`).

Key files:

- `skills/sr-search/src/stockroom/dashboard/__main__.py` — `DEFAULT_PORT = 58008`
- `skills/sr-search/src/stockroom/dashboard/server.py` — `serve(port=58008)` default
- `skills/sr-search/tests/test_dashboard_cli.py`, `test_dashboard_identity.py` — assertions/fixtures; added `test_default_port_is_58008` as TDD entry
- `skills/sr-dashboard/SKILL.md`, `docs/using.md`, `memory-bank/techContext.md` — advertised default URL/port

Dual literals (`DEFAULT_PORT` and `serve` default) left as-is under dead-simple scope; not refactored to a single source.

## TESTING

- TDD: `test_default_port_is_58008` red then green
- Targeted dashboard CLI/identity tests green
- `make ci` green (511 passed, 3 skipped)
- Spot-check: no remaining `6767` outside archive/active/uv.lock/.pytest_cache
- `/niko-qa` PASS — no fixes required

## LESSONS LEARNED

- Digit-sequence find/replace against a lockfile is unsafe even when the token looks unique; always path-scope constant swaps away from hash artifacts.
- Port-scoped identity/launcher behavior means changing the default leaves old listeners orphaned by design — acceptable when the brief forbids migration.

## PROCESS IMPROVEMENTS

Nothing notable for this mechanical constant swap. Preflight correctly flagged unrelated dirty `REUSE.toml` as out of scope.

## TECHNICAL IMPROVEMENTS

Optional later: collapse `DEFAULT_PORT` and the `serve(port=...)` default into one source of truth. Not worth a redesign for this change.

## NEXT STEPS

None.
