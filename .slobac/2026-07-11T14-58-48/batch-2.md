## Findings

### `tests/test_ingest_sources.py`

- **Location:** `tests/test_ingest_sources.py:102` (`test_mtime_is_naive_utc`)
- **Smell:** `conditional-logic`
- **Rationale:** The UTC-vs-local assertion is gated behind `if datetime.now().astimezone().utcoffset() != timezone.utc...`, so on UTC hosts that branch never asserts and the path is vacuous by omission. Signal: IfStatement inside the test body whose consequent asserts and whose alternate is absent. See https://texarkanine.github.io/slobac/taxonomy/conditional-logic/.
- **Prescribed remediation:** Split into two tests (or pin a frozen non-UTC offset via monkeypatch) so the naive-UTC ≠ local-naive claim always asserts; remove the platform/timezone `if` from the body.
- **Why this isn't a false positive:** Not a runner-native skip (`pytest.mark.skipif`); the branch silently passes as green on UTC machines rather than reporting skipped.

- **Location:** `tests/test_ingest_sources.py:102` (`test_mtime_is_naive_utc`)
- **Smell:** `implementation-coupled`
- **Rationale:** The body calls `sources._mtime(path)`, reaching a single-underscore private helper rather than the public discovery surface that stamps `DiscoveredSession.mtime`. Signal: `_private_method(` access from a separate test module. See https://texarkanine.github.io/slobac/taxonomy/implementation-coupled/.
- **Prescribed remediation:** Drive mtime through `sources.discover(...)` (or another public API) and assert on the returned `DiscoveredSession.mtime`; keep `_mtime` untested as a private detail.
- **Why this isn't a false positive:** Not same-module / `#[cfg(test)]`-style sanctioned access: the test module is outside `stockroom.ingest.sources` and bypasses the public discover contract.

### `tests/test_migrate_cli.py`

- **Location:** `tests/test_migrate_cli.py:64` (`test_migrate_help_mentions_migration`)
- **Smell:** `loose-text-oracle`
- **Rationale:** Help success is identified only by `assert "migrat" in result.stdout.lower()` — a shared-token substring that would also match unrelated help text ("migratory", "immigrate", typos). The match is the sole semantic oracle for which help was shown. See https://texarkanine.github.io/slobac/taxonomy/loose-text-oracle/.
- **Prescribed remediation:** Assert structured argparse contract (subcommand/epilog presence via exact known phrases or parsed help sections), or pin a full golden of `--help` output as an explicit presentation contract.
- **Why this isn't a false positive:** Not a typed-error-plus-datum pattern: there is no error type/code; the truncated token alone identifies the claim.

### `tests/test_packaging.py`

- **Location:** `tests/test_packaging.py:76` (`test_skeleton_skill_front_matter`)
- **Smell:** `prose-pin`
- **Rationale:** Reads committed `SKILL.md` and only asserts front-matter `name`/`description` are non-empty strings — the packaging/front-matter non-empty checklist called out as a prose-pin weaker sibling. See https://texarkanine.github.io/slobac/taxonomy/prose-pin/.
- **Prescribed remediation:** Validate front-matter via a schema/parser (required keys, types, length bounds, name slug rules) or delete the vacuous non-empty string checks if schema validation lives elsewhere.
- **Why this isn't a false positive:** Not architectural fitness-function negatives and not prose-as-SUT temp fixtures: this asserts on committed skill prose fields without structural schema validation.

### `tests/test_query.py`

- **Location:** `tests/test_query.py:68` (`test_run_query_no_con_is_read_only`)
- **Smell:** `conditional-logic`
- **Rationale:** Uses `try: run_query(...); except duckdb.Error: raised = True` then `assert raised` without an explicit fail after a successful return — if the write unexpectedly succeeds, the except never runs and only the final flag assert fails after mutation risk; the classic shape is try/catch without fail-after-SUT. Prefer `pytest.raises`. See https://texarkanine.github.io/slobac/taxonomy/conditional-logic/.
- **Prescribed remediation:** Replace with `with pytest.raises(duckdb.Error): run_query("CREATE TABLE ...")` (and optionally assert warehouse state unchanged).
- **Why this isn't a false positive:** Not the cured `try: sut(); assert.fail(...)` form — there is no fail-after-success path inside the try.

### `tests/test_query_cli.py`

- **Location:** `tests/test_query_cli.py:59` (`test_query_happy_path_prints_result`)
- **Smell:** `vacuous-assertion`
- **Rationale:** After a successful exit, content oracles are only `"n" in result.stdout` and `"1" in result.stdout`, which many wrong TSV/table/json renderings would still satisfy. Sibling tests already lock exact TSV lines; this happy-path oracle stays weak. See https://texarkanine.github.io/slobac/taxonomy/vacuous-assertion/.
- **Prescribed remediation:** Assert the exact default TSV lines (e.g. `splitlines() == ["n", "1"]`) or parse structured output; drop bare substring presence checks.
- **Why this isn't a false positive:** Not a side-effect-absence contract: the name claims printed column/value output, which deserves a strong structured oracle.

- **Location:** `tests/test_query_cli.py:73` (`test_query_proves_end_to_end_queryability`)
- **Smell:** `deliverable-fossils`
- **Rationale:** Name/docstring frame the test as a "Phase-1 proof" deliverable rather than a durable product claim about DISTINCT harness visibility after ingest. Signal: sprint/phase vocabulary in title/docstring. See https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/.
- **Prescribed remediation:** Rename to a behavior claim (e.g. `test_query_lists_ingested_cursor_and_claude_harnesses`) and drop Phase-1 checklist wording from the docstring.
- **Why this isn't a false positive:** Not domain vocabulary: "Phase-1 proof" names the delivery milestone, not a product entity that appears in the SUT.

- **Location:** `tests/test_query_cli.py:183` (`test_query_reads_sql_from_stdin`)
- **Smell:** `vacuous-assertion`
- **Rationale:** Success is asserted via returncode plus only `assert "1" in result.stdout`, accepting any stdout that happens to contain the digit. See https://texarkanine.github.io/slobac/taxonomy/vacuous-assertion/.
- **Prescribed remediation:** Assert the exact TSV (or JSON) rendering of `SELECT 1 AS n` from stdin, matching the stronger format tests in this file.
- **Why this isn't a false positive:** Not under-specified-title alone: the body itself under-delivers on a knowable exact result shape.

### `tests/test_schedule_cli.py`

- **Location:** `tests/test_schedule_cli.py:70` (`test_help_documents_actions_and_time_flag`)
- **Smell:** `deliverable-fossils`
- **Rationale:** Docstring leads with checklist id `B15:` tying the help contract to a plan item rather than the product help surface. Signal: checklist/ticket vocabulary in the docstring. See https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/.
- **Prescribed remediation:** Strip the `B15:` prefix; keep a behavior-shaped docstring about documented actions and `--time` default.
- **Why this isn't a false positive:** Not product domain vocabulary: B15 is work-item labeling from the schedule plan, not an SUT term.

- **Location:** `tests/test_schedule_cli.py:79` (`test_invalid_action_is_clean_error`)
- **Smell:** `deliverable-fossils`
- **Rationale:** Docstring leads with `B15:` checklist vocabulary for an argparse invalid-action behavior. See https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/.
- **Prescribed remediation:** Rename/reword the docstring to the product claim (invalid action → exit 2, no traceback) without the plan-item prefix.
- **Why this isn't a false positive:** Not under-specified title alone: the fossil id is explicit work vocabulary.

- **Location:** `tests/test_schedule_cli.py:87` (`test_status_against_empty_environment`)
- **Smell:** `deliverable-fossils`
- **Rationale:** Docstring leads with `B15:` framing status CLI coverage as a checklist item. See https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/.
- **Prescribed remediation:** Drop the `B15:` prefix; describe the empty-environment status facts the body verifies.
- **Why this isn't a false positive:** Not domain synonymy: B15 is plan numbering, not schedule product language.

### `tests/test_shim.py`

- **Location:** `tests/test_shim.py:272` (`TestRectify::test_always_ensures_engine_env`)
- **Smell:** `deliverable-fossils`
- **Rationale:** Docstring opens with `B4:` checklist labeling for ensure_engine_env-on-rectify behavior. See https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/.
- **Prescribed remediation:** Remove the `B4:` fossil prefix; keep the behavior claim about always ensuring the engine env.
- **Why this isn't a false positive:** Not a refactor-safety property test: B4 names a work item, not the behavior under test.

### `tests/test_shim_cli.py`

- **Location:** `tests/test_shim_cli.py:88` (`test_ensure_env_exits_zero_without_owner`)
- **Smell:** `deliverable-fossils`
- **Rationale:** Docstring opens with `B5:` checklist id for ensure-env CLI wiring. See https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/.
- **Prescribed remediation:** Strip `B5:`; name the durable CLI contract (ensure-env without --owner exits 0 and invokes ensure).
- **Why this isn't a false positive:** Not product API versioning vocabulary; B5 is delivery checklist labeling.

### `tests/test_shim_runtime.py`

- **Location:** `tests/test_shim_runtime.py:157` (`test_env_not_ready_one_line_owner_remedy`)
- **Smell:** `deliverable-fossils`
- **Rationale:** Docstring opens with `B6:` checklist id for the duckdb-not-ready refuse path. See https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/.
- **Prescribed remediation:** Remove `B6:`; keep the one-line owner-remedy refuse behavior description.
- **Why this isn't a false positive:** Not domain vocabulary appearing in the SUT; B6 is plan-item scaffolding.

### `tests/test_skill_hygiene.py`

- **Location:** `tests/test_skill_hygiene.py:55` (`test_read_skills_document_exact_text_raw_detail`)
- **Smell:** `prose-pin`
- **Rationale:** Treats committed wrapper `SKILL.md` bytes as oracle via `assert "--detail raw" in text` and `assert "--format json" in text` — feature-mention pins that fail on editorial rewrites without proving the flags work. See https://texarkanine.github.io/slobac/taxonomy/prose-pin/.
- **Prescribed remediation:** Delete the mention pins; re-ground on CLI/behavioral tests that exercise `--detail raw` / `--format json` (already present in query/semantic suites) or move wording policy to docs-lint.
- **Why this isn't a false positive:** Not a documented forbidden-token fitness function (those are the sibling `test_wrapper_skill_has_no_invocation_plumbing`); these are positive feature-mention checklist asserts.

### `tests/test_torch_cli.py`

- **Location:** `tests/test_torch_cli.py:33` (`test_freeze_invokes_freeze_torch`)
- **Smell:** `deliverable-fossils`
- **Rationale:** Docstring leads with `F6:` ticket/checklist vocabulary for freeze CLI wiring. See https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/.
- **Prescribed remediation:** Drop `F6:`; rename/reword to the product claim (freeze forwards --app-dir/--index to freeze_torch).
- **Why this isn't a false positive:** Not a regression bug-id comment citing a fixed defect with behavioral body; F6 is sprint checklist labeling.

No findings for scope `tautology-theatre`.

No findings for scope `over-specified-mock`.

No findings for scope `pseudo-tested`.

No findings for scope `monolithic-test-file`.

No findings for scope `naming-lies`.

No findings for scope `presentation-coupled`.

No findings for scope `shared-state`.

No findings for scope `mystery-guest`.

No findings for scope `rotten-green`.

## Behavior Summaries

| File | Line | Test ID | Behavior | Tier | Smells Found |
|------|------|---------|----------|------|--------------|
| tests/test_ingest_sources.py | 20 | test_root_env_overrides | STOCKROOM_*_ROOT env overrides win over defaults. Calls sources.cursor_root/claude_root after monkeypatch.setenv. Asserts equality to tmp override paths. Targets stockroom.ingest discovery/writer contracts on fixtures or migrated_con. | integration | — |
| tests/test_ingest_sources.py | 28 | test_default_roots_when_env_absent | Absent env vars fall back to ~/.cursor/projects and ~/.claude/projects. Calls cursor_root/claude_root after delenv. Asserts Path.home()-based defaults. Targets stockroom.ingest discovery/writer contracts on fixtures or migrated_con. | integration | — |
| tests/test_ingest_sources.py | 36 | test_discover_cursor_enumerates_sessions_and_subagents | Cursor discover lists fixture sessions with verbatim project_id and subagent paths. Calls sources.discover('cursor', cursor_root). Asserts stem set, project_ids, harness, and subagent filenames. Targets stockroom.ingest discovery/writer contracts on fixtures or migrated_con. | integration | — |
| tests/test_ingest_sources.py | 64 | test_discover_claude_enumerates_sessions_and_subagents | Claude discover lists top-level sessions and attaches subagents. Calls sources.discover('claude', claude_root). Asserts stem set, project_id, and parent subagent name. Targets stockroom.ingest discovery/writer contracts on fixtures or migrated_con. | integration | — |
| tests/test_ingest_sources.py | 85 | test_discover_results_are_sorted_deterministically | Discovery returns path-sorted sessions. Calls sources.discover('cursor', ...). Asserts session_path list equals sorted(paths). Targets stockroom.ingest discovery/writer contracts on fixtures or migrated_con. | integration | — |
| tests/test_ingest_sources.py | 102 | test_mtime_is_naive_utc | File mtime helper returns naive UTC matching utc_from_timestamp. Calls sources._mtime on a temp file. Asserts tzinfo is None, equality to expected, and optional ≠ local-naive off-UTC. Targets stockroom.ingest discovery/writer contracts on fixtures or migrated_con. | unit | conditional-logic, implementation-coupled |
| tests/test_ingest_sources.py | 116 | test_watermark_full_returns_all | select_new with no watermark returns all items. Calls sources.select_new(..., last_mtime=None). Asserts identity with input list. Targets stockroom.ingest discovery/writer contracts on fixtures or migrated_con. | unit | — |
| tests/test_ingest_sources.py | 125 | test_watermark_incremental_filters_by_mtime | Incremental select keeps only newer-than-mark sessions. Calls select_new with last_mtime mid-range. Asserts only the newer DiscoveredSession remains. Targets stockroom.ingest discovery/writer contracts on fixtures or migrated_con. | unit | — |
| tests/test_ingest_sources.py | 135 | test_watermark_tie_break_on_path | Equal mtime uses path > last_path tie-break. Calls select_new with identical mtimes. Asserts only the higher path is selected. Targets stockroom.ingest discovery/writer contracts on fixtures or migrated_con. | unit | — |
| tests/test_ingest_writer.py | 71 | test_write_session_inserts_all_rows | write_session inserts one session, two messages, one tool_call. Calls writer.write_session. Asserts table counts 1/2/1. Targets stockroom.ingest discovery/writer contracts on fixtures or migrated_con. | integration | — |
| tests/test_ingest_writer.py | 81 | test_write_session_expands_message_identity | Ordinals expand to session#ordinal ids and parents. Calls write_session then SELECT message_id/parent_id. Asserts s1#0/None and s1#1/s1#0. Targets stockroom.ingest discovery/writer contracts on fixtures or migrated_con. | integration | — |
| tests/test_ingest_writer.py | 94 | test_write_session_tool_call_links_and_inputs | Tool call links to turn with JSON input and provenance. Calls write_session; SELECTs tool_calls. Asserts message_id, ordinal, name, path, source id. Targets stockroom.ingest discovery/writer contracts on fixtures or migrated_con. | integration | — |
| tests/test_ingest_writer.py | 108 | test_write_session_is_idempotent | Re-write same session is delete-then-insert without PK errors. Calls write_session twice. Asserts stable counts 1/2/1. Targets stockroom.ingest discovery/writer contracts on fixtures or migrated_con. | integration | — |
| tests/test_ingest_writer.py | 121 | test_write_session_reingest_reflects_new_content | Grown session re-ingest replaces message set. Calls write_session then append+rewrite. Asserts messages count == 3. Targets stockroom.ingest discovery/writer contracts on fixtures or migrated_con. | integration | — |
| tests/test_ingest_writer.py | 132 | test_write_session_persists_source_mtime | source_mtime round-trips on sessions row. Calls write_session with expected datetime. Asserts stored source_mtime equals expected. Targets stockroom.ingest discovery/writer contracts on fixtures or migrated_con. | integration | — |
| tests/test_ingest_writer.py | 144 | test_first_write_seeds_message_first_seen_at | New messages seed first_seen_at from source_mtime. Calls write_session. Asserts both message first_seen_at values equal observed mtime. Targets stockroom.ingest discovery/writer contracts on fixtures or migrated_con. | integration | — |
| tests/test_ingest_writer.py | 156 | test_append_carries_existing_first_seen_and_stamps_only_new_message | Append preserves old first_seen_at and stamps only new row. Calls write_session twice with grown messages. Asserts per-message first_seen_at triple. Targets stockroom.ingest discovery/writer contracts on fixtures or migrated_con. | integration | — |
| tests/test_ingest_writer.py | 179 | test_unchanged_rewrite_preserves_every_first_seen_at | Unchanged rewrite keeps original first_seen_at despite newer mtime. Calls write_session twice. Asserts both rows still first_seen. Targets stockroom.ingest discovery/writer contracts on fixtures or migrated_con. | integration | — |
| tests/test_ingest_writer.py | 194 | test_write_session_persists_cursor_models_list | Cursor models LIST persists natively. Calls write_session with models list. Asserts SELECT models equals input list. Targets stockroom.ingest discovery/writer contracts on fixtures or migrated_con. | integration | — |
| tests/test_ingest_writer.py | 212 | test_write_session_no_truncation_at_rest | Large tool_input contents round-trip whole. Calls write_session with 20k string. Asserts stored JSON path equals big string. Targets stockroom.ingest discovery/writer contracts on fixtures or migrated_con. | integration | — |
| tests/test_ingest_writer.py | 242 | test_rewriting_session_cascades_embedding_delete | Rewrite drops this session's embeddings only. Calls write_session, inserts embeddings, rewrites. Asserts remaining owner_id == {s2#0}. Targets stockroom.ingest discovery/writer contracts on fixtures or migrated_con. | integration | — |
| tests/test_ingest_writer.py | 278 | test_update_watermark_default_updated_at_is_utc_now | Default updated_at uses utc_now (monkeypatched). Calls update_watermark. Asserts _sync_state.updated_at == frozen. Targets stockroom.ingest discovery/writer contracts on fixtures or migrated_con. | integration | — |
| tests/test_ingest_writer.py | 298 | test_update_watermark_upserts_one_row_per_source | Watermark upsert updates single _sync_state row. Calls update_watermark twice. Asserts one row with latest mtime/path. Targets stockroom.ingest discovery/writer contracts on fixtures or migrated_con. | integration | — |
| tests/test_licensing.py | 45 | test_reuse_lint_passes | Entire tree is REUSE-compliant. Runs `reuse lint`. Asserts returncode == 0. Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | contract | — |
| tests/test_licensing.py | 51 | test_code_inside_skill_resolves_agpl | Skill-tree .py resolves AGPL not PPL-S. Reads license_map for __init__.py. Asserts AGPL present and PPL-S absent. Locks packaging/licensing invariants over committed repo artifacts. | contract | — |
| tests/test_licensing.py | 59 | test_prompt_skill_resolves_ppls | SKILL.md resolves to PPL-S. Reads license_map. Asserts LicenseRef-PPL-S in map. Locks packaging/licensing invariants over committed repo artifacts. | contract | — |
| tests/test_licensing.py | 66 | test_skill_reference_resolves_ppls | skills/** references resolve PPL-S only. Reads license_map for system-model.md. Asserts PPL-S present, AGPL absent. Locks packaging/licensing invariants over committed repo artifacts. | contract | — |
| tests/test_licensing.py | 74 | test_fixture_readme_stays_agpl | Fixture README stays AGPL. Reads license_map. Asserts exact {AGPL-3.0-or-later}. Locks packaging/licensing invariants over committed repo artifacts. | contract | — |
| tests/test_licensing.py | 81 | test_shell_inside_skill_resolves_agpl | Shim template .sh is AGPL software. Reads license_map. Asserts AGPL present, PPL-S absent. Locks packaging/licensing invariants over committed repo artifacts. | contract | — |
| tests/test_licensing.py | 89 | test_authored_dashboard_assets_resolve_agpl | Authored dashboard assets/tests are AGPL. Iterates license_map targets. Asserts each == {AGPL-3.0-or-later}. Locks packaging/licensing invariants over committed repo artifacts. | contract | — |
| tests/test_licensing.py | 106 | test_vendored_chartjs_resolves_only_mit | Vendored Chart.js is MIT-only. Reads license_map. Asserts == {MIT}. Locks packaging/licensing invariants over committed repo artifacts. | contract | — |
| tests/test_licensing.py | 115 | test_vendored_markdown_it_resolves_only_mit | Vendored markdown-it is MIT-only. Reads license_map. Asserts == {MIT}. Locks packaging/licensing invariants over committed repo artifacts. | contract | — |
| tests/test_lock_hermetic.py | 58 | test_lock_has_no_torch_cuda_nvidia | Lock has no torch/CUDA/nvidia package names. Scans uv.lock packages. Asserts offenders list empty. Locks packaging/licensing invariants over committed repo artifacts. | contract | — |
| tests/test_lock_hermetic.py | 68 | test_lock_packages_are_pypi_with_hashes | Non-virtual packages resolve from PyPI with hashes. Iterates lock packages. Asserts registry URL and sdist/wheel hashes. Locks packaging/licensing invariants over committed repo artifacts. | contract | — |
| tests/test_lock_hermetic.py | 89 | test_lock_manifest_overrides_torch | Lock manifest records impossible-marker torch override. Reads manifest.overrides. Asserts torch marker python_full_version < '3'. Locks packaging/licensing invariants over committed repo artifacts. | contract | — |
| tests/test_lock_hermetic.py | 97 | test_pyproject_encodes_torch_contract | pyproject encodes python>=3.11, package=false, torch override. Reads pyproject tool.uv. Asserts requires-python, package False, override string. Locks packaging/licensing invariants over committed repo artifacts. | contract | — |
| tests/test_lock_hermetic.py | 107 | test_lock_is_not_stale | Committed lock matches pyproject via uv lock --locked. Runs subprocess uv. Asserts returncode == 0. Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | contract | — |
| tests/test_migrate_cli.py | 38 | test_migrate_fresh_home_bootstraps_warehouse | Fresh STOCKROOM_HOME migrate creates warehouse at head. Runs python -m stockroom.migrate. Asserts exit 0, warehouse file, head version in stdout. Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | unit | — |
| tests/test_migrate_cli.py | 51 | test_migrate_is_idempotent | Second migrate is no-op same stdout. Runs migrate twice. Asserts exit 0, identical stdout, empty stderr. Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | unit | — |
| tests/test_migrate_cli.py | 64 | test_migrate_help_mentions_migration | --help exits 0 and mentions migration. Runs migrate --help. Asserts returncode 0 and 'migrat' substring in stdout. Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | unit | loose-text-oracle |
| tests/test_migrate_runner.py | 40 | test_current_version_is_zero_when_bookkeeping_table_absent | Absent schema_version table reads as version 0. Calls migrate.current_version(mem_con). Asserts == 0. Migration discovery/runner/CLI contract against mem_con or throwaway HOME. | integration | — |
| tests/test_migrate_runner.py | 47 | test_ensure_schema_version_table_is_idempotent | ensure_schema_version_table creates table and is re-runnable. Calls ensure twice. Asserts table present and current_version 0. Migration discovery/runner/CLI contract against mem_con or throwaway HOME. | integration | — |
| tests/test_migrate_runner.py | 69 | test_apply_pending_applies_all_packaged_on_fresh_db | Fresh DB applies packaged migrations 1..5. Calls ensure_vss+apply_pending. Asserts applied list, product tables, version 5, bookkeeping row for 0001. Migration discovery/runner/CLI contract against mem_con or throwaway HOME. | integration | — |
| tests/test_migrate_runner.py | 97 | test_apply_pending_is_idempotent | Re-apply on current DB returns []. Calls apply_pending twice. Asserts second [], version 5, row_count == first applied len. Migration discovery/runner/CLI contract against mem_con or throwaway HOME. | integration | — |
| tests/test_migrate_runner.py | 113 | test_apply_pending_applies_in_ascending_order | Temp migrations apply low-to-high. Calls apply_pending(dir). Asserts [1,2], tables alpha/beta, version 2. Migration discovery/runner/CLI contract against mem_con or throwaway HOME. | integration | — |
| tests/test_migrate_runner.py | 131 | test_apply_pending_rolls_back_a_failing_migration | Failing migration rolls back atomically. Calls apply_pending expecting duckdb.Error. Asserts version 1, good present, half absent, no v2 row. Migration discovery/runner/CLI contract against mem_con or throwaway HOME. | integration | — |
| tests/test_migrate_runner.py | 162 | test_apply_pending_no_op_when_db_is_ahead | DB ahead of packaged set is untouched. Seeds version 5 then apply_pending({1}). Asserts [], version 5, alpha absent. Migration discovery/runner/CLI contract against mem_con or throwaway HOME. | integration | — |
| tests/test_migrations_discovery.py | 15 | test_migrations_dir_holds_packaged_0001 | migrations_dir is package dir shipping 0001. Calls migrations_dir(). Asserts equals schema_sql_path.parent and 0001 file exists. Migration discovery/runner/CLI contract against mem_con or throwaway HOME. | unit | — |
| tests/test_migrations_discovery.py | 21 | test_discover_returns_packaged_migrations_ascending | Default discover yields contiguous ascending versions. Calls discover(). Asserts first Migration path, versions 1..n, includes {1,2}. Migration discovery/runner/CLI contract against mem_con or throwaway HOME. | unit | — |
| tests/test_migrations_discovery.py | 34 | test_discover_ignores_non_conforming_filenames | Only NNNN_slug.sql names are migrations. Calls discover(tmp_path) with decoys. Asserts exactly Migration(1..) and Migration(2..). Migration discovery/runner/CLI contract against mem_con or throwaway HOME. | unit | — |
| tests/test_migrations_discovery.py | 52 | test_discover_orders_numerically_not_lexically | Versions sort numerically so 0010 after 0002. Calls discover(tmp_path). Asserts [1,2,10]. Migration discovery/runner/CLI contract against mem_con or throwaway HOME. | unit | — |
| tests/test_packaging.py | 47 | test_both_manifests_carry_required_keys | Cursor/Claude manifests carry required plugin keys. Reads cursor_manifest/claude_manifest. Asserts REQUIRED_MANIFEST_KEYS ⊆ keys. Locks packaging/licensing invariants over committed repo artifacts. | contract | — |
| tests/test_packaging.py | 55 | test_manifests_name_and_license | Manifests name stockroom and declare AGPL. Reads both manifests. Asserts name and license fields. Locks packaging/licensing invariants over committed repo artifacts. | contract | — |
| tests/test_packaging.py | 65 | test_manifest_versions_match | Manifest versions lockstep. Compares cursor vs claude version. Asserts equality. Locks packaging/licensing invariants over committed repo artifacts. | contract | — |
| tests/test_packaging.py | 70 | test_cursor_skills_pointer_resolves | Cursor skills pointer is ./skills/ and dir exists. Reads manifest+repo_root. Asserts skills path and is_dir. Locks packaging/licensing invariants over committed repo artifacts. | contract | — |
| tests/test_packaging.py | 76 | test_skeleton_skill_front_matter | sr-search SKILL.md has non-empty name/description front-matter. Parses YAML FM. Asserts isinstance str and truthy. Locks packaging/licensing invariants over committed repo artifacts. | contract | prose-pin |
| tests/test_packaging.py | 99 | test_all_version_sources_in_lockstep | Plugin + release-please versions agree. Compares manifests to release_manifest['.']. Asserts equality. Locks packaging/licensing invariants over committed repo artifacts. | contract | — |
| tests/test_packaging.py | 122 | test_manifests_point_at_hook_configs | Manifests point at harness hook JSON paths. Reads cursor_manifest/claude_manifest hooks fields. Asserts ./hooks/cursor-hooks.json and ./hooks/claude-hooks.json. Locks packaging/licensing invariants over committed repo artifacts. | contract | — |
| tests/test_packaging.py | 182 | test_cursor_hook_schema_and_combined_command | Cursor sessionStart is one timed command with rectify→dashboard contract. Reads cursor_hooks. Asserts schema, timeout 300, PATH/drain/rectify/dashboard shape. Locks packaging/licensing invariants over committed repo artifacts. | contract | — |
| tests/test_packaging.py | 215 | test_claude_hook_schema_and_combined_command | Claude SessionStart is one silenced timed rectify→dashboard command. Reads claude_hooks. Asserts type/timeout and shared command shape. Locks packaging/licensing invariants over committed repo artifacts. | contract | — |
| tests/test_packaging.py | 234 | test_release_config_syncs_both_manifests | release-please extra-files sync $.version into both manifests. Reads release_config. Asserts both jsonpath targets present. Locks packaging/licensing invariants over committed repo artifacts. | contract | — |
| tests/test_query.py | 19 | test_run_query_returns_columns_and_rows | run_query returns columns and rows for SELECT 1. Calls run_query(..., con=). Asserts QueryResult columns ['n'] and rows [(1,)]. Focuses on the named behavior's return values and side effects. | integration | — |
| tests/test_query.py | 27 | test_run_query_over_real_schema | Query over seeded sessions returns harness/project_id. Inserts row then run_query. Asserts columns and rows tuple. Focuses on the named behavior's return values and side effects. | integration | — |
| tests/test_query.py | 42 | test_run_query_empty_result_keeps_columns | Empty result keeps column names. Calls run_query WHERE 1=0. Asserts columns and empty rows. Focuses on the named behavior's return values and side effects. | integration | — |
| tests/test_query.py | 52 | test_run_query_opens_read_only_when_no_con | Owns-connection path opens warehouse and returns count. Seeds via warehouse.open then run_query without con. Asserts rows [(1,)]. Focuses on the named behavior's return values and side effects. | integration | — |
| tests/test_query.py | 68 | test_run_query_no_con_is_read_only | Owns-connection path rejects writes. Calls run_query CREATE TABLE in try/except. Asserts duckdb.Error raised flag. Focuses on the named behavior's return values and side effects. | integration | conditional-logic |
| tests/test_query_cli.py | 59 | test_query_happy_path_prints_result | Ingest then SELECT 1 AS n prints column/value. Runs ingest + stockroom.query subprocess. Asserts exit 0 and weak 'n'/'1' substrings in stdout. Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | integration | vacuous-assertion |
| tests/test_query_cli.py | 73 | test_query_proves_end_to_end_queryability | Ingested warehouse DISTINCT harness query names cursor and claude. Runs ingest + query. Asserts exit 0 and both harness names in stdout. Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | integration | deliverable-fossils |
| tests/test_query_cli.py | 89 | test_query_invalid_sql_is_clean_error | Invalid SQL exits nonzero without traceback. Runs query SELEKT 1. Asserts nonzero, nonempty stderr, no Traceback. Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | integration | — |
| tests/test_query_cli.py | 102 | test_query_read_only_rejects_writes | Write SQL rejected and table never created. Runs CREATE then SELECT scratch. Asserts both nonzero; no Traceback on write. Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | integration | — |
| tests/test_query_cli.py | 117 | test_query_missing_warehouse_is_friendly | Missing warehouse hints stockroom ingest. Runs query against empty home. Asserts nonzero, no Traceback, exact hint substring. Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | unit | — |
| tests/test_query_cli.py | 129 | test_query_empty_sql_is_rejected | Whitespace-only SQL rejected without opening warehouse. Runs query '   '. Asserts nonzero and nonempty stderr. Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | unit | — |
| tests/test_query_cli.py | 138 | test_query_detail_controls_truncation | Default elides wide cell; full keeps it; compact shorter. Runs same SQL at three detail levels. Asserts ellipsis/absence/count comparisons. Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | integration | — |
| tests/test_query_cli.py | 162 | test_query_invalid_detail_is_rejected | Unknown --detail exits 2. Runs --detail bogus. Asserts returncode 2 and nonempty stderr. Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | unit | — |
| tests/test_query_cli.py | 170 | test_query_detail_raw_is_accepted | --detail raw preserves newlines in json. Runs json+raw SQL with chr(10). Asserts parsed rows cell == 'a\nb'. Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | integration | — |
| tests/test_query_cli.py | 183 | test_query_reads_sql_from_stdin | '-' sentinel reads SQL from stdin. Runs query - with stdin SELECT. Asserts exit 0 and '1' in stdout. Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | integration | vacuous-assertion |
| tests/test_query_cli.py | 195 | test_query_default_output_is_tsv | Default format is exact TSV without table art. Runs SELECT 1 AS a, 2 AS b. Asserts exact lines and no pipe/trailer. Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | integration | — |
| tests/test_query_cli.py | 210 | test_query_format_table_restores_table | --format table restores column-aligned table and (1 row) trailer. Runs query subprocess. Asserts pipe separators present and endswith (1 row). Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | integration | — |
| tests/test_query_cli.py | 223 | test_query_format_json_parses | --format json emits parseable columns/rows object. Runs json query. Asserts json.loads equals expected structure. Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | integration | — |
| tests/test_query_cli.py | 235 | test_query_invalid_format_is_rejected | Unknown --format exits 2. Runs --format bogus. Asserts returncode 2 and nonempty stderr. Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | unit | — |
| tests/test_render.py | 42 | test_default_format_is_tsv | Module DEFAULT_FORMAT is tsv. Reads render.DEFAULT_FORMAT. Asserts == 'tsv'. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 47 | test_query_defaults_to_tsv | format_query default matches explicit tsv. Calls format_query twice. Asserts string equality. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 54 | test_semantic_defaults_to_tsv | format_semantic default matches explicit tsv. Calls format_semantic twice. Asserts string equality. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 64 | test_query_tsv_header_and_rows | Query tsv is header + tab-separated row. Calls format_query(..., fmt=tsv). Asserts splitlines and no pipes. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 71 | test_query_tsv_empty_is_header_only | Empty query tsv is header only. Calls format_query with []. Asserts ['n']. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 77 | test_query_tsv_truncates_wide_cell_by_default | Wide tsv cell elided at default detail. Calls format_query with 600 x's. Asserts ELISION present, full absent. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 84 | test_query_tsv_full_detail_keeps_whole_cell | detail=full keeps whole tsv cell. Calls format_query detail full. Asserts full string present, ELISION absent. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 94 | test_query_json_shape | Query json is columns/rows of stringified cells. json.loads(format_query(...)). Asserts exact object. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 100 | test_query_json_empty_rows | Empty query json has empty rows list. Parses format_query. Asserts columns/rows shape. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 106 | test_query_json_none_is_native_null | SQL NULL becomes JSON null. Parses format_query with None. Asserts rows [[None]]. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 112 | test_query_json_truncates_by_default | Wide json cell elided by default. Parses format_query. Asserts ELISION in cell and inequality to full. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 120 | test_query_json_full_detail_keeps_whole_cell | detail=full keeps whole json cell. Parses format_query. Asserts cell == 600 x's. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 128 | test_query_json_raw_preserves_newlines | detail=raw keeps newlines in json cell. Parses format_query. Asserts original multiline string. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 137 | test_query_json_full_still_collapses_whitespace | detail=full collapses whitespace in json. Parses format_query. Asserts 'a b c'. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 145 | test_query_json_raw_null_stays_null | NULL under raw still JSON null. Parses format_query. Asserts [[None]]. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 154 | test_query_table_renders_header_and_row | Single-row table shows column, value, and (1 row) trailer. Calls render.format_query(..., fmt=table). Asserts 'n'/'1' present and endswith trailer. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 162 | test_query_table_empty_keeps_header_and_zero_trailer | Empty table keeps header and (0 rows). Calls format_query. Asserts 'n' and (0 rows). Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 169 | test_query_table_row_count_trailer_pluralizes | Two-row table trailer pluralizes. Calls format_query. Asserts endswith (2 rows). Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 175 | test_query_table_renders_none_as_null | None cell renders as NULL literal. Calls format_query table. Asserts 'NULL' in out. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 181 | test_query_table_truncates_wide_cell_by_default | Wide table cell elided by default. Calls format_query. Asserts ELISION present, full absent. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 188 | test_query_table_full_detail_keeps_whole_cell | detail=full keeps whole table cell. Calls format_query. Asserts full present, ELISION absent. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 198 | test_semantic_tsv_header_and_no_trailer | Semantic tsv header fixed; no result trailer. Calls format_semantic. Asserts header line and absences. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 206 | test_semantic_tsv_score_is_similarity | score is 1-distance (1.000 for zero). Calls format_semantic. Asserts '1.000' in out. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 212 | test_semantic_tsv_truncates_long_text | Long semantic preview elided in tsv. Calls format_semantic. Asserts ELISION, full absent. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 219 | test_semantic_tsv_full_detail_keeps_whole_text | detail=full keeps whole semantic tsv text. Calls format_semantic. Asserts full z*600 present. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 228 | test_semantic_json_shape | Semantic json results[] carries identifying fields. Parses format_semantic. Asserts rank/score/harness/ids/role/text. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 256 | test_semantic_json_empty_results | No hits → {results: []}. Parses format_semantic([]). Asserts exact object. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 262 | test_semantic_json_score_is_a_number | score is JSON number 1-distance. Parses format_semantic distance 0.25. Asserts 0.75 float. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 270 | test_semantic_json_text_truncated_by_default | Default json text elided. Parses format_semantic. Asserts ELISION in text. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 276 | test_semantic_json_full_detail_keeps_whole_text | detail=full keeps whole semantic json text. Parses format_semantic. Asserts text == z*600. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 284 | test_semantic_json_raw_preserves_newlines | detail=raw keeps newlines in semantic json text. Parses format_semantic. Asserts original string. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 293 | test_semantic_json_full_still_collapses_whitespace | detail=full collapses semantic json whitespace. Parses format_semantic. Asserts 'a b c'. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 304 | test_semantic_table_shows_similarity_score | Table reports similarity score from distance. Calls format_semantic table. Asserts score label and 1.0. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 311 | test_semantic_table_previews_text_single_line | Long multiline text previewed single-line truncated. Calls format_semantic table. Asserts collapsed preview, full absent. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 320 | test_semantic_table_full_detail_keeps_whole_text | detail=full keeps entire table text. Calls format_semantic. Asserts z*500 present, ELISION absent. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_render.py | 329 | test_semantic_table_empty_is_zero_results | Empty semantic table shows 0 results trailer. Calls format_semantic([]). Asserts '0 results' in out. Pure renderer check with literal inputs (no DB/torch). | unit | — |
| tests/test_schedule.py | 98 | test_payload_invokes_shim_with_log_redirection | Payload runs date/ingest/embed with log redirect. Calls render_payload. Asserts exact command string with nightly.log. Uses injected crontab/launchctl seams; never touches a real crontab. | unit | deliverable-fossils? |
| tests/test_schedule.py | 109 | test_payload_is_home_aware | Log path follows caller home. Calls render_payload twice. Asserts distinct home log paths embedded. Uses injected crontab/launchctl seams; never touches a real crontab. | unit | deliverable-fossils? |
| tests/test_schedule.py | 118 | test_payload_contains_no_engine_path_and_no_percent | Payload has no %, engine path, or python -m. Calls render_payload. Asserts absences of %, sr-search, python -m. Uses injected crontab/launchctl seams; never touches a real crontab. | integration | deliverable-fossils? |
| tests/test_schedule.py | 128 | test_parse_time_accepts_strict_hh_mm | Strict HH:MM parses to (h,m). Calls parse_time on valid samples. Asserts tuples including defaults. Uses injected crontab/launchctl seams; never touches a real crontab. | unit | deliverable-fossils? |
| tests/test_schedule.py | 137 | test_parse_time_rejects_malformed_values | Malformed times raise ValueError naming HH:MM. Calls parse_time under pytest.raises(match=HH:MM). Uses injected crontab/launchctl seams; never touches a real crontab. | unit | deliverable-fossils? |
| tests/test_schedule.py | 144 | test_cli_rejects_bad_time_with_clean_error | CLI --time malformed exits 2 naming HH:MM. Calls schedule.main. Asserts SystemExit 2 and HH:MM in stderr. Uses injected crontab/launchctl seams; never touches a real crontab. | unit | deliverable-fossils? |
| tests/test_schedule.py | 159 | test_install_on_empty_crontab_writes_exactly_the_managed_block | Empty crontab install writes exact managed block. Calls cron_install with FakeCrontab. Asserts markers, PATH, schedule fields, one write. Uses injected crontab/launchctl seams; never touches a real crontab. | unit | deliverable-fossils? |
| tests/test_schedule.py | 193 | test_install_preserves_foreign_lines_byte_for_byte | Foreign crontab lines preserved byte-for-byte. Calls cron_install. Asserts written.startswith(foreign) and markers present. Uses injected crontab/launchctl seams; never touches a real crontab. | unit | deliverable-fossils? |
| tests/test_schedule.py | 213 | test_reinstall_replaces_the_managed_block | Reinstall replaces schedule time leaving one block. Calls cron_install twice. Asserts single markers, new time, foreign prefix. Uses injected crontab/launchctl seams; never touches a real crontab. | unit | deliverable-fossils? |
| tests/test_schedule.py | 244 | test_install_refuses_when_shim_not_on_path | Missing shim refuses without write. Calls cron_install(which_without_shim). Asserts refused, no writes, remedy tokens. Uses injected crontab/launchctl seams; never touches a real crontab. | unit | deliverable-fossils? |
| tests/test_schedule.py | 262 | test_install_with_dead_daemon_still_writes_but_warns | Dead daemon still installs with warning; live has none. Calls cron_install twice. Asserts installed+warnings vs empty warnings. Uses injected crontab/launchctl seams; never touches a real crontab. | unit | deliverable-fossils? |
| tests/test_schedule.py | 292 | test_default_daemon_check_matches_cron_or_crond | default_daemon_check accepts crond and degrades failures. Calls with fake runners. Asserts True/False/False. Uses injected crontab/launchctl seams; never touches a real crontab. | integration | deliverable-fossils? |
| tests/test_schedule.py | 311 | test_remove_strips_block_and_preserves_foreign_lines | remove strips managed block only. Calls cron_install then cron_remove. Asserts content == foreign. Uses injected crontab/launchctl seams; never touches a real crontab. | unit | deliverable-fossils? |
| tests/test_schedule.py | 331 | test_remove_is_a_clean_noop_without_a_block | remove without block/crontab is noop. Calls cron_remove twice. Asserts noop and no writes. Uses injected crontab/launchctl seams; never touches a real crontab. | unit | deliverable-fossils? |
| tests/test_schedule.py | 345 | test_status_reports_installed_daemon_and_log_facts | status reports schedule, daemon running, log path. Calls cron_status after install. Asserts installed/schedule/daemon/log substrings. Uses injected crontab/launchctl seams; never touches a real crontab. | unit | deliverable-fossils? |
| tests/test_schedule.py | 365 | test_status_reports_not_installed_and_dead_daemon | status without block reports not installed + dead daemon. Calls cron_status. Asserts not installed, daemon not running, log fact. Uses injected crontab/launchctl seams; never touches a real crontab. | unit | deliverable-fossils? |
| tests/test_schedule.py | 378 | test_cron_install_creates_the_log_directory | install creates <home>/logs. Calls cron_install on fresh home. Asserts logs is_dir. Uses injected crontab/launchctl seams; never touches a real crontab. | unit | deliverable-fossils? |
| tests/test_schedule.py | 405 | test_launchd_install_writes_a_valid_plist | launchd install writes plist and bootout/bootstrap. Calls launchd_install. Asserts plist fields, paths, launchctl call order. Uses injected crontab/launchctl seams; never touches a real crontab. | unit | deliverable-fossils? |
| tests/test_schedule.py | 445 | test_launchd_install_tolerates_bootout_failure | Failing bootout still bootstraps successfully. Calls launchd_install(bootout_fails). Asserts installed and call sequence. Uses injected crontab/launchctl seams; never touches a real crontab. | unit | deliverable-fossils? |
| tests/test_schedule.py | 462 | test_launchd_reinstall_rewrites_single_plist | Reinstall rewrites single plist with new interval. Calls launchd_install twice. Asserts one plist and Hour/Minute 22/15. Uses injected crontab/launchctl seams; never touches a real crontab. | unit | deliverable-fossils? |
| tests/test_schedule.py | 490 | test_launchd_remove_boots_out_and_deletes | remove bootouts and deletes; absent is noop. Calls launchd_remove twice. Asserts removed/noop and call lists. Uses injected crontab/launchctl seams; never touches a real crontab. | unit | deliverable-fossils? |
| tests/test_schedule.py | 514 | test_launchd_status_reads_the_plist | status reads interval from plist when installed. Calls launchd_status before/after install. Asserts not installed then installed/03:30/log. Uses injected crontab/launchctl seams; never touches a real crontab. | unit | deliverable-fossils? |
| tests/test_schedule.py | 541 | test_launchd_install_refuses_when_shim_not_on_path | launchd missing-shim refusal writes nothing. Calls launchd_install(which_without_shim). Asserts refused, no plist, no calls. Uses injected crontab/launchctl seams; never touches a real crontab. | unit | deliverable-fossils? |
| tests/test_schedule.py | 565 | test_unsupported_platform_refuses_naming_wsl | Unsupported Windows platform exits 1 naming WSL. Calls schedule.main(system=Windows). Asserts code 1, WSL in err, no Traceback. Uses injected crontab/launchctl seams; never touches a real crontab. | unit | deliverable-fossils? |
| tests/test_schedule_cli.py | 70 | test_help_documents_actions_and_time_flag | --help documents actions and --time default. Runs schedule --help subprocess. Asserts exit 0 and token presence. Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | integration | deliverable-fossils |
| tests/test_schedule_cli.py | 79 | test_invalid_action_is_clean_error | Invalid action exits 2 without traceback. Runs schedule bogus. Asserts returncode 2, no Traceback. Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | integration | deliverable-fossils |
| tests/test_schedule_cli.py | 87 | test_status_against_empty_environment | status against stub crontab reports not-installed facts. Runs schedule status (Linux). Asserts exit 0, not installed, daemon:, log path. Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | integration | deliverable-fossils |
| tests/test_schema_0001.py | 126 | test_all_five_tables_exist | 0001 creates exactly five product tables. Uses schema_con. Asserts table name set equality. Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_schema_0001.py | 146 | test_composite_pk_rejects_duplicate | Composite PK rejects second row with same key. Parametrized insert+duplicate. Asserts ConstraintException. Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_schema_0001.py | 192 | test_not_null_columns_reject_null | Required columns reject NULL. Parametrized NULL insert. Asserts ConstraintException. Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_schema_0001.py | 281 | test_messages_reconstruct_in_conversation_order | Messages reconstruct by ordinal order. Inserts ordered messages; SELECTs. Asserts ordered roles/ids. Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_schema_0001.py | 292 | test_tools_attach_to_their_turn_in_block_order | Tool calls attach to turn in block ordinal order. Inserts tools; SELECTs. Asserts message_id/ordinal ordering. Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_schema_0001.py | 309 | test_parent_chain_threads_and_root_is_null | parent_id threads conversation; root null. Inserts chain; SELECTs. Asserts parent linkages. Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_schema_0001.py | 329 | test_subagent_links_to_parent_session | Subagent session links to parent. Inserts parent/child sessions. Asserts is_subagent/parent_session_id fields. Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_schema_0001.py | 360 | test_model_grain_split_no_faking | Model fields stay honest at session vs message grain. Inserts split models. Asserts typed columns without fabricating. Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_schema_0001.py | 429 | test_token_columns_are_typed_and_aggregatable | Token columns are typed numerics aggregatable. Inserts tokens; SUM query. Asserts aggregate values. Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_schema_0001.py | 495 | test_tool_input_json_path_extraction | tool_input JSON path extraction works. Inserts JSON; SELECTs ->>. Asserts extracted field. Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_schema_0001.py | 530 | test_models_list_is_native_and_queryable | models LIST is native/queryable. Inserts list; SELECTs. Asserts list contents. Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_schema_0001.py | 551 | test_no_truncation_at_rest | Large text/tool_input stored untruncated. Inserts big payloads; SELECTs. Asserts full length/content. Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_schema_0001.py | 594 | test_embeddings_vector_is_fixed_384 | embeddings.vector is FLOAT[384]. Inserts valid/invalid sizes. Asserts accept 384 / reject wrong dim. Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_schema_0001.py | 618 | test_harness_neutral_accepts_unknown_harness | Unknown harness label accepted. Inserts harness='other'. Asserts row stored. Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_schema_0001.py | 636 | test_empty_string_text_is_distinct_from_null | Empty string text ≠ NULL. Inserts '' and NULL. Asserts distinct stored values. Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_schema_0001.py | 654 | test_assistant_turn_with_zero_tools | Assistant turn may have zero tools. Inserts message without tools. Asserts tool_calls count 0. Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_schema_0001.py | 677 | test_many_tools_have_contiguous_ordinals | Many tools keep contiguous ordinals. Inserts N tools. Asserts ordinal sequence. Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_schema_0001.py | 711 | test_identical_content_gets_distinct_ids_via_ordinal | Identical content still distinct via ordinal ids. Inserts dup text different ordinals. Asserts distinct message_ids. Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_schema_0001.py | 739 | test_unicode_and_large_json_tool_input | Unicode and large JSON tool_input round-trip. Inserts unicode/large JSON. Asserts stored equality. Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_schema_0001.py | 810 | test_schema_matches_locked_snapshot | Applied 0001 schema matches golden snapshot. Introspects schema_con vs 0001_snapshot.json. Asserts deep equality. Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_schema_0002.py | 41 | test_0002_adds_project_id_and_drops_project_path | After ``0002`` the ``sessions`` table gains ``project_id`` and loses ``project_path``; ``cwd`` is retained. Asserts: "project_id" in cols; "project_path" not in cols. Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_schema_0002.py | 53 | test_0002_preserves_existing_rows_and_children | A ``0001``-shape session (and its message) survives the migration with its ``cwd`` intact — the migration is data-preserving, not destructive. Asserts: row == ("s1", "/home/user/proj"); child_count == 1. | contract | — |
| tests/test_schema_0002.py | 79 | test_0002_preexisting_project_id_is_null | A row migrated from ``0001`` has ``project_id IS NULL`` (no backfill — the verbatim slug is unrecoverable from the lossy old value; a ``--full`` re-ingest repopulates it). Asserts: project_id is None. | contract | — |
| tests/test_schema_0002.py | 132 | test_migrated_schema_matches_cumulative_snapshot | The post-``0002`` schema byte-matches the committed cumulative snapshot. Asserts: SNAPSHOT_PATH.is_file(), f"golden schema snapshot missing: {SNAPSHO...; actual == expected. Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_schema_0003.py | 57 | test_0003_creates_hnsw_cosine_index | After the chain, a non-primary HNSW index sits on ``embeddings(vector)``. Asserts: len(rows) == 1; index_name == HNSW_INDEX_NAME. Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_schema_0003.py | 75 | test_0003_index_supports_knn_and_live_delete | The live index answers cosine KNN and accepts a delete (persistence on). Asserts: nearest == "s#0"; mem_con.execute("SELECT count(*) FROM embeddings").fetchone()[0] == 2. Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_schema_0003.py | 149 | test_migrated_schema_matches_0003_snapshot | The post-``0003`` schema (columns + PKs + indexes) byte-matches the golden. Asserts: SNAPSHOT_PATH.is_file(), f"golden schema snapshot missing: {SNAPSHO...; actual == expected. Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_schema_0004.py | 40 | test_0004_adds_session_source_mtime | Sessions gain the source transcript mtime used as activity provenance. Asserts: "source_mtime" in _columns(mem_con, "sessions"). Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_schema_0004.py | 46 | test_0004_adds_message_first_seen_at | Messages gain the time at which stockroom first observed them. Asserts: "first_seen_at" in _columns(mem_con, "messages"). Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_schema_0004.py | 54 | test_migrated_schema_matches_0004_snapshot | The cumulative post-``0004`` schema byte-matches its golden snapshot. Asserts: SNAPSHOT_PATH.is_file(), f"golden schema snapshot missing: {SNAPSHO...; actual == expected. Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_schema_0005.py | 27 | test_0005_clears_sync_state_watermarks | Applying ``0005`` nulls ``last_mtime`` / ``last_path`` but keeps rows. SUT: discover. Asserts: row is not None; row[0] is None. Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_schema_0005.py | 50 | test_0005_schema_matches_0004_snapshot | Cumulative DDL after ``0005`` still matches the ``0004`` golden. Asserts: actual == expected. Contract-level check against DuckDB schema/migration artifacts under tests/fixtures where applicable. | contract | — |
| tests/test_semantic.py | 53 | test_embed_query_applies_query_prefix_by_default | ``embed_query`` prepends the bge query instruction before encoding. SUT: semantic.embed_query, semantic.QUERY_PREFIX. Asserts: spy.seen == [semantic.QUERY_PREFIX + "how do I open the warehouse"]. Uses FakeEncoder/migrated warehouse fixtures unless torch-gated. | unit | — |
| tests/test_semantic.py | 60 | test_embed_query_prefix_can_be_overridden | An explicit ``prefix=""`` encodes the bare query (the deterministic-fake path). SUT: semantic.embed_query. Asserts: spy.seen == ["bare query"]. Uses FakeEncoder/migrated warehouse fixtures unless torch-gated. | unit | — |
| tests/test_semantic.py | 67 | test_embed_query_returns_single_vector | A query encodes to exactly one ``EMBED_DIM``-length vector (never chunked). SUT: semantic.embed_query, embed.EMBED_DIM. Asserts: len(vector) == embed.EMBED_DIM; all(isinstance(component, float) for component in vector). | unit | — |
| tests/test_semantic.py | 77 | test_search_returns_nearest_message_first | The message whose text the query matches is rank 1 with ~zero distance. SUT: embed.embed_pending, semantic.run_semantic_search. Asserts: hits[0].message_id == target_id; hits[0].rank == 1. Uses FakeEncoder/migrated warehouse fixtures unless torch-gated. | integration | — |
| tests/test_semantic.py | 93 | test_search_ranks_and_distances_ascending | Hits are ranked 1..n in nondecreasing cosine distance. SUT: embed.embed_pending, semantic.run_semantic_search. Asserts: [hit.rank for hit in hits] == list(range(1, len(hits) + 1)); distances == sorted(distances). | integration | — |
| tests/test_semantic.py | 110 | test_search_dedups_multichunk_owner_to_one_row | A multi-chunk message appears exactly once (its nearest chunk), not per-chunk. SUT: embed.chunk_text, embed.embed_pending, semantic.run_semantic_search. Asserts: len(embed.chunk_text(long_text)) > 1  # guard: really multi-chunk; owners.count(long_id) == 1. | integration | — |
| tests/test_semantic.py | 127 | test_search_respects_limit | ``limit`` caps the result count; a limit above the corpus returns all. SUT: embed.embed_pending, semantic.run_semantic_search. Asserts: len(capped) == 3; len(everything) == 7. Uses FakeEncoder/migrated warehouse fixtures unless torch-gated. | integration | — |
| tests/test_semantic.py | 144 | test_search_hit_carries_owner_join_fields | Each hit carries the joined ``messages`` columns (harness/session/role/text). SUT: embed.embed_pending, semantic.run_semantic_search. Asserts: hit.harness == "cursor"; hit.session_id == "conv-x". Uses FakeEncoder/migrated warehouse fixtures unless torch-gated. | integration | — |
| tests/test_semantic.py | 170 | test_search_no_embeddings_returns_empty | A warehouse with messages but no embeddings yields no hits (no error). SUT: semantic.run_semantic_search. Asserts: hits == []. Uses FakeEncoder/migrated warehouse fixtures unless torch-gated. | integration | — |
| tests/test_semantic.py | 181 | test_search_owns_connection_opens_read_only | With no ``con``, search opens the warehouse itself (read-only) and returns hits. SUT: warehouse.open, embed.embed_pending, writer.close. Asserts: len(hits) == 1; hits[0].text == "seeded for the owns-connection path". | integration | — |
| tests/test_semantic.py | 206 | test_cli_prints_ranked_results | The CLI embeds the query with the injected fake and prints the match (tsv). SUT: warehouse.open, embed.embed_pending, writer.close. Asserts: code == 0; "the unique findable phrase" in out. Uses FakeEncoder/migrated warehouse fixtures unless torch-gated. | integration | — |
| tests/test_semantic.py | 224 | test_cli_limit_flag_caps_results | ``-k N`` caps the number of printed result rows (counted as tsv data lines). SUT: warehouse.open, embed.embed_pending, writer.close. Asserts: code == 0; len(out.splitlines()) == 1 + 2. Uses FakeEncoder/migrated warehouse fixtures unless torch-gated. | integration | — |
| tests/test_semantic.py | 242 | test_cli_default_output_is_tsv | Default CLI output is tsv header without table art or trailer. Calls semantic.main. Asserts header line and absence of pipes/(1 result). Uses FakeEncoder/migrated warehouse fixtures unless torch-gated. | integration | — |
| tests/test_semantic.py | 259 | test_cli_format_table_shows_trailer | --format table restores human table and (1 result) trailer. Calls semantic.main. Asserts pipe separators and (1 result) present. Uses FakeEncoder/migrated warehouse fixtures unless torch-gated. | integration | — |
| tests/test_semantic.py | 277 | test_cli_format_json_parses | ``--format json`` emits a parseable ``{results: [...]}`` object with the match. SUT: warehouse.open, embed.embed_pending, writer.close. Asserts: code == 0; parsed["results"][0]["text"] == "the unique findable phrase". | integration | — |
| tests/test_semantic.py | 295 | test_cli_invalid_format_rejected | An unknown ``--format`` value exits 2 (argparse) before the encoder is built. SUT: semantic.main. Asserts: exc.value.code == 2. Uses FakeEncoder/migrated warehouse fixtures unless torch-gated. | integration | — |
| tests/test_semantic.py | 302 | test_cli_detail_full_prints_whole_text | ``--detail full`` renders the entire (long) message text; the default elides it. SUT: warehouse.open, embed.embed_pending, writer.close. Asserts: code == 0; "w" * 400 in full_out. Uses FakeEncoder/migrated warehouse fixtures unless torch-gated. | integration | — |
| tests/test_semantic.py | 328 | test_cli_detail_raw_preserves_newlines | ``--detail raw`` with ``--format json`` keeps newlines in ``text``. SUT: warehouse.open, embed.embed_pending, writer.close. Asserts: code == 0; parsed["results"][0]["text"] == text. Uses FakeEncoder/migrated warehouse fixtures unless torch-gated. | integration | — |
| tests/test_semantic.py | 347 | test_cli_missing_warehouse_is_friendly | No warehouse → exit 1 with a 'run ingest' hint, encoder never constructed. SUT: semantic.main. Asserts: code == 1; "run `stockroom ingest` first" in capsys.readouterr().err. Uses FakeEncoder/migrated warehouse fixtures unless torch-gated. | integration | — |
| tests/test_semantic.py | 356 | test_cli_empty_query_rejected | An empty / whitespace-only query exits 2 and never builds the encoder. SUT: semantic.main. Asserts: code == 2; capsys.readouterr().err.strip() != "". Uses FakeEncoder/migrated warehouse fixtures unless torch-gated. | integration | — |
| tests/test_semantic.py | 363 | test_cli_nonpositive_limit_rejected | A non-positive ``--limit`` exits 2 with a message and never builds the encoder. SUT: semantic.main. Asserts: code == 2; capsys.readouterr().err.strip() != "". Uses FakeEncoder/migrated warehouse fixtures unless torch-gated. | integration | — |
| tests/test_semantic.py | 373 | test_real_model_search_ranks_paraphrase_first | End-to-end with the real ``BgeEncoder``: a paraphrase finds its message. SUT: embed.BgeEncoder, embed.embed_pending, semantic.run_semantic_search. Asserts: hits[0].message_id == dog_id. Uses FakeEncoder/migrated warehouse fixtures unless torch-gated. | integration | — |
| tests/test_shim.py | 77 | TestRender::test_bakes_app_dir_and_owner_markers | The rendered script carries the baked APP_DIR and the machine-readable owner header marker. SUT: shim.render. Asserts: str(engine_dir) in text; "# STOCKROOM_OWNER=cursor" in text. Exercises shim policy/render/runtime seams with tmp destinations only. | unit | — |
| tests/test_shim.py | 85 | TestRender::test_carries_torch_safe_exec_contract | The exec line preserves the torch-safe run contract verbatim: --no-sync, --no-config, PYTHONPATH to src, exec into uv run. SUT: shim.render. Asserts: "--no-sync" in text; "--no-config" in text. Exercises shim policy/render/runtime seams with tmp destinations only. | unit | — |
| tests/test_shim.py | 97 | TestRender::test_stamps_generator_version | The generator version is stamped so drift is diagnosable. SUT: shim.render. Asserts: stockroom.__version__ in text. Exercises shim policy/render/runtime seams with tmp destinations only. | unit | — |
| tests/test_shim.py | 102 | TestRender::test_remedy_matches_owner | The staleness remedy names the owner-appropriate healing action: sr-initialize for harness owners, make shim for dev. SUT: shim.render. Asserts: "sr-initialize" in shim.render(engine_dir, "cursor"); "make shim" in shim.render(engine_dir, "dev"). | unit | — |
| tests/test_shim.py | 116 | TestInstall::test_absent_dest_writes_executable_rendered_shim | Install to an absent dest writes the rendered content, 0o755. SUT: shim.install, shim.render. Asserts: report.action == "installed"; dest.is_file(). Exercises shim policy/render/runtime seams with tmp destinations only. | unit | — |
| tests/test_shim.py | 126 | TestInstall::test_reinstall_same_owner_replaces_cleanly | Re-install by the same owner is idempotent: content replaced, no temp debris left beside the dest. SUT: shim.install, shim.render. Asserts: report.action == "installed"; dest.read_text() == shim.render(other_engine_dir, "cursor"). | unit | — |
| tests/test_shim.py | 137 | TestInstall::test_foreign_owner_alive_dir_refuses_untouched | A different owner may not replace a shim whose baked dir is alive; the refusal names the incumbent owner and the dest is untouched. SUT: shim.install. Asserts: report.action == "refused"; "cursor" in report.reason. | unit | — |
| tests/test_shim.py | 149 | TestInstall::test_foreign_owner_alive_dir_refuses_even_with_takeover | --takeover never clobbers a *working* foreign shim. SUT: shim.install. Asserts: report.action == "refused". Exercises shim policy/render/runtime seams with tmp destinations only. | unit | — |
| tests/test_shim.py | 157 | TestInstall::test_foreign_owner_dead_dir_requires_explicit_takeover | A dead incumbent is still refused by default; takeover succeeds only with the explicit flag. SUT: shim.install, shim.render. Asserts: refused.action == "refused"; taken.action == "installed". Exercises shim policy/render/runtime seams with tmp destinations only. | unit | — |
| tests/test_shim.py | 170 | TestInstall::test_corrupt_header_treated_as_foreign | An existing dest with an unreadable header is foreign: refuse without --takeover, never crash. SUT: shim.install. Asserts: report.action == "refused". Exercises shim policy/render/runtime seams with tmp destinations only. | unit | — |
| tests/test_shim.py | 180 | TestInstall::test_default_app_dir_is_running_engine_dir | The default app dir resolves to the engine dir that contains the running stockroom package (run-in-place layout). SUT: shim.default_app_dir. Asserts: (app_dir / "pyproject.toml").is_file(); (app_dir / "src" / "stockroom").is_dir(). | unit | — |
| tests/test_shim.py | 189 | TestInstallPathAndVerify::test_dest_off_path_reports_and_skips_verify | When the dest dir is not on PATH the report says so and the --version verify is skipped with a reason. SUT: shim.install. Asserts: report.action == "installed"; report.path_ok is False. Exercises shim policy/render/runtime seams with tmp destinations only. | unit | — |
| tests/test_shim.py | 203 | TestInstallPathAndVerify::test_dest_on_path_attempts_version_verify | When the dest dir is on PATH the install verifies via ``stockroom --version`` through PATH (stub uv makes it succeed). SUT: shim.install. Asserts: report.action == "installed"; report.path_ok is True. | unit | — |
| tests/test_shim.py | 237 | TestRectify::test_owner_match_content_drift_rewrites | A moved root (or template change) under the same owner re-bakes. SUT: shim.install, shim.rectify, shim.render. Asserts: report.action == "rectified"; dest.read_text() == shim.render(other_engine_dir, "cursor"). | unit | — |
| tests/test_shim.py | 246 | TestRectify::test_owner_match_identical_content_noop | Steady state: rendered content already matches → no write. SUT: shim.install, shim.rectify. Asserts: report.action == "noop"; dest.stat().st_mtime_ns == mtime. Exercises shim policy/render/runtime seams with tmp destinations only. | unit | — |
| tests/test_shim.py | 256 | TestRectify::test_owner_mismatch_never_touches_foreign_shim | Rectify by a non-owner is a silent no-op even when content differs. SUT: shim.install, shim.rectify. Asserts: report.action == "noop"; dest.read_text() == before. Exercises shim policy/render/runtime seams with tmp destinations only. | unit | — |
| tests/test_shim.py | 266 | TestRectify::test_absent_dest_never_creates | Rectify never creates a missing shim — install is the only gate. SUT: shim.rectify. Asserts: report.action == "noop"; not dest.exists(). Exercises shim policy/render/runtime seams with tmp destinations only. | unit | — |
| tests/test_shim.py | 272 | TestRectify::test_always_ensures_engine_env | Rectify always runs ensure_engine_env for app_dir first, including when the dest is absent (path noop). SUT: shim.rectify. Asserts: report.action == "noop"; len(calls) == 1. Exercises shim policy/render/runtime seams with tmp destinations only. | unit | deliverable-fossils |
| tests/test_shim_cli.py | 72 | test_help_documents_subactions_and_flags | ``--help`` exits 0 and names install, rectify, ensure-env, and the flags. Asserts: result.returncode == 0, result.stderr; token in result.stdout. Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | integration | — |
| tests/test_shim_cli.py | 88 | test_ensure_env_exits_zero_without_owner | ``ensure-env`` does not require ``--owner`` and exits 0 on noop/sync. SUT: shim.main. Asserts: code == 0; calls, "ensure_engine_env must be invoked". Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | unit | deliverable-fossils |
| tests/test_shim_cli.py | 115 | test_install_writes_shim_and_exits_zero | A clean install via the CLI exits 0 and writes the executable shim. Asserts: result.returncode == 0, result.stderr; dest.is_file(). Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | integration | — |
| tests/test_shim_cli.py | 134 | test_install_refusal_exits_nonzero | An ownership refusal exits nonzero and names the incumbent owner. Asserts: first.returncode == 0, first.stderr; second.returncode != 0. Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | integration | — |
| tests/test_shim_cli.py | 163 | test_rectify_noop_exits_zero | Rectify against an absent dest is the hook steady state: exit 0. Asserts: result.returncode == 0, result.stderr; not dest.exists(). Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | integration | — |
| tests/test_shim_cli.py | 179 | test_rectify_rebakes_moved_root | Integration chain: install as owner A at root X, rectify to root Y → shim bakes Y; a foreign rectify leaves it unchanged. Asserts: install.returncode == 0, install.stderr; moved.returncode == 0, moved.stderr. | integration | — |
| tests/test_shim_cli.py | 223 | test_dispatcher_forwards_shim | ``python -m stockroom shim --help`` forwards to the module's help. Asserts: result.returncode == 0, result.stderr; "rectify" in result.stdout. Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | integration | — |
| tests/test_shim_cli.py | 239 | test_full_chain_dispatcher_install_then_exec | Torch-free full chain: install through the real dispatcher, then run the written shim with a stub uv and observe the exec contract. Asserts: installed.returncode == 0, installed.stderr; run.returncode == 0, run.stderr. | integration | — |
| tests/test_shim_import_graph.py | 49 | test_importing_stockroom_shim_does_not_load_duckdb | ``import stockroom.shim`` must not pull DuckDB (heal entrypoint). Exercises shim policy/render/runtime seams with tmp destinations only. | unit | — |
| tests/test_shim_import_graph.py | 54 | test_dispatcher_shim_help_does_not_load_duckdb | ``python -m stockroom shim --help`` must not import DuckDB. SUT: main. Asserts: code == 0, code; "; 'duckdb' not in sys.modules, ". Exercises shim policy/render/runtime seams with tmp destinations only. | integration | — |
| tests/test_shim_import_graph.py | 77 | test_importing_torch_source_does_not_load_duckdb | ``import stockroom.torch_source`` must not pull DuckDB (heal stack). Exercises shim policy/render/runtime seams with tmp destinations only. | unit | — |
| tests/test_shim_runtime.py | 70 | test_valid_dir_execs_with_torch_safe_contract | A valid baked dir execs into uv with the full torch-safe contract: correct --project APP_DIR, PYTHONPATH to $APP_DIR/src, --no-sync, --no-config, and the dispatcher module. Asserts: result.returncode == 0, result.stderr; pythonpath == f"{engine_dir}/src". | integration | — |
| tests/test_shim_runtime.py | 95 | test_args_forwarded_verbatim_including_spaces | Arguments (including embedded spaces) reach uv verbatim, unsplit. Asserts: result.returncode == 0, result.stderr; args[-2:] == ["query", "SELECT 1 AS n"]. Exercises shim policy/render/runtime seams with tmp destinations only. | integration | — |
| tests/test_shim_runtime.py | 120 | test_invalid_dir_one_line_owner_remedy | A dead baked dir refuses with exactly one stderr line naming the owner-appropriate remedy, nonzero exit, nothing on stdout. Asserts: result.returncode != 0; result.stdout == "". Exercises shim policy/render/runtime seams with tmp destinations only. | integration | — |
| tests/test_shim_runtime.py | 139 | test_uv_missing_one_line_127 | Without uv on PATH the shim fails with the single documented line and exit 127. Asserts: result.returncode == 127; len(stderr_lines) == 1. Exercises shim policy/render/runtime seams with tmp destinations only. | integration | — |
| tests/test_shim_runtime.py | 157 | test_env_not_ready_one_line_owner_remedy | Pyproject present but venv cannot import duckdb → refuse with remedy before ``uv run --no-sync`` (no silent empty-venv path). Asserts: result.returncode != 0; result.stdout == "". Exercises shim policy/render/runtime seams with tmp destinations only. | integration | deliverable-fossils |
| tests/test_skill_hygiene.py | 40 | test_wrapper_skill_has_no_invocation_plumbing | Wrapper SKILL.md contains no pre-shim invocation tokens (architectural fitness). Reads skills/*/SKILL.md. Asserts FORBIDDEN_TOKENS list empty of offenses. Focuses on the named behavior's return values and side effects. | contract | — |
| tests/test_skill_hygiene.py | 55 | test_read_skills_document_exact_text_raw_detail | Read-surface skills mention --detail raw and --format json in committed SKILL.md. Reads skill_md text. Asserts both substrings present. Focuses on the named behavior's return values and side effects. | contract | prose-pin |
| tests/test_smoke.py | 8 | test_harness_runs | The pytest harness executes and a trivial assertion holds. Asserts: 1 + 1 == 2. Suite-bootstrap smoke only. | smoke | — |
| tests/test_smoke.py | 13 | test_import_stockroom_exposes_version | ``import stockroom`` succeeds and exposes a string ``__version__``. Asserts: isinstance(stockroom.__version__, str); stockroom.__version__. Suite-bootstrap smoke only. | smoke | — |
| tests/test_timestamps.py | 10 | test_utc_now_returns_naive_utc | ``utc_now`` is timezone-naive and matches a frozen UTC instant. SUT: timestamps.utc_now, utc_now. Asserts: tz is timezone.utc; got.tzinfo is None. Unit coverage of stockroom.timestamps helpers. | unit | — |
| tests/test_timestamps.py | 26 | test_utc_from_timestamp_returns_naive_utc | POSIX epoch seconds become naive UTC wall clock. SUT: timestamps.utc_from_timestamp, utc_from_timestamp. Asserts: got.tzinfo is None; got == datetime(2026, 7, 10, 3, 22, 0). Unit coverage of stockroom.timestamps helpers. | unit | — |
| tests/test_timestamps.py | 35 | test_to_utc_naive_converts_aware_offset_to_utc | Aware stamps convert to UTC before tzinfo is dropped. SUT: timestamps.to_utc_naive, to_utc_naive. Asserts: got.tzinfo is None; got == datetime(2026, 7, 10, 3, 22, 0). Unit coverage of stockroom.timestamps helpers. | unit | — |
| tests/test_timestamps.py | 44 | test_to_utc_naive_leaves_naive_unchanged | Naive values are already treated as UTC and returned as-is. SUT: timestamps.to_utc_naive, to_utc_naive. Asserts: timestamps.to_utc_naive(naive) is naive. Unit coverage of stockroom.timestamps helpers. | unit | — |
| tests/test_torch_cli.py | 33 | test_freeze_invokes_freeze_torch | ``torch freeze --app-dir --index`` invokes freeze_torch (stubbed). SUT: torch_cli.main. Asserts: code == 0; Path(seen["app_dir"]) == Path(app_dir). Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | unit | deliverable-fossils |
| tests/test_torch_cli.py | 60 | test_freeze_defaults_app_dir_to_engine | Preflight: omitted --app-dir uses default_app_dir(). SUT: torch_cli.main. Asserts: code == 0; seen == [engine]. Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | unit | — |
| tests/test_torch_cli.py | 80 | test_freeze_propagates_soft_failure | CLI exits non-zero when freeze_torch soft-fails. SUT: torch_cli.main. Asserts: code == 1. Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | unit | — |
| tests/test_torch_cli.py | 101 | test_freeze_rejects_bad_url | Test freeze rejects bad url. SUT: torch_cli.main, torch_source.read_freeze_path. Asserts: code == 2; torch_source.read_freeze_path() is None. Integration-style coverage of argv/exit-code contracts for the stockroom CLI surface. | unit | — |
| tests/test_torch_cli.py | 107 | test_record_action_removed | ``record`` is no longer a valid action (prefer freeze). SUT: torch_cli.main, torch_source.read_index, torch_source.read_freeze_path. Asserts: torch_source.read_index() is None; torch_source.read_freeze_path() is None. | unit | — |
