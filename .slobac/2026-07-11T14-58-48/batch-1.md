## Findings

### `tests-js/dashboard-core.test.mjs`

- **Location:** `tests-js/dashboard-core.test.mjs` :: `PANEL_HELP documents efficiency and first-prompt buckets` (line 506)
- **Smell:** `loose-text-oracle`
- **Rationale:** Asserts unanchored keyword/regex presence in `PANEL_HELP` strings (`/short/i`, `/100|500/`, etc.). Many unrelated help texts sharing those tokens would pass. Signal: substring/regex as sole meaning proxy on free text. See https://texarkanine.github.io/slobac/taxonomy/loose-text-oracle/.
- **Prescribed remediation:** If help copy is the product, lock a reviewed full-string golden; otherwise assert structured bucket metadata the UI consumes rather than English tokens.
- **Why this isn't a false positive:** Not a full golden/approval of UX copy — these are underdetermined token matches, the loose-text failure mode rather than presentation-coupled exactness.

### `tests/test_dashboard_metrics.py`

- **Location:** `tests/test_dashboard_metrics.py` :: `test_iso_appends_z_for_naive_utc_datetimes` (line 150)
- **Smell:** `implementation-coupled`
- **Rationale:** Calls private helper `metrics._iso` directly and asserts its return. Signal: underscore-prefixed access reaching past the public metrics API. See https://texarkanine.github.io/slobac/taxonomy/implementation-coupled/.
- **Prescribed remediation:** Assert datetime wire formatting through public metrics payloads (e.g. overview/session fields that emit ISO strings) or promote a deliberate public helper if the format is a contract.
- **Why this isn't a false positive:** Not sanctioned VisibilityForTesting / same-module convention — this is cross-package reach into a private function from the test suite.

### `tests/test_dashboard_server.py`

- **Location:** `tests/test_dashboard_server.py` :: `test_session_api_returns_detail_and_client_errors` (line 275)
- **Smell:** `loose-text-oracle`
- **Rationale:** Missing `session` vs missing `harness` both return HTTP 400; which failure occurred is identified only by `"session" in payload["error"]` / `"harness" in payload["error"]`. Shared-token substrings underdetermine meaning. Signal: message substring as sole identifier of failure kind beside a shared status. See https://texarkanine.github.io/slobac/taxonomy/loose-text-oracle/.
- **Prescribed remediation:** Prefer stable error codes/fields (e.g. `error_code` / missing-param name) or exact structured error payloads; keep message text supplementary only.
- **Why this isn't a false positive:** Not typed-primary+supplementary-datum — status is identical for both failures, so the substring *is* the discriminator.

### `tests/test_dashboard_static.py`

- **Location:** `tests/test_dashboard_static.py` :: `test_session_pane_toolbar_and_bubble_layout_contracts` (line 142)
- **Smell:** `presentation-coupled`
- **Rationale:** Pins exact emoji toolbar copy (`⬅️ Back to metrics`, etc.) and cosmetic CSS (`width: 90%`, `align-self: flex-end`, overflow rules) in raw HTML/source strings. Signal: raw HTML/CSS presentation assertions when the real claim is layout/affordance semantics. See https://texarkanine.github.io/slobac/taxonomy/presentation-coupled/.
- **Prescribed remediation:** Assert semantic structure (button ids/roles, turn class presence, tool scroll container) via the HTML parser; keep presentation goldens only if rendering is the deliberate contract and tier them separately.
- **Why this isn't a false positive:** Not a formatter/ANSI library whose deliverable is the string — this is dashboard shell markup; cosmetic CSS/emoji edits would fail without behavior change.

- **Location:** `tests/test_dashboard_static.py` :: `test_write_read_chart_aria_describes_ratio_not_absolute_volumes` (line 209)
- **Smell:** `loose-text-oracle`
- **Rationale:** Oracle is `"ratio" in label or "share" in label` (plus a negative phrase). Opposite or unrelated aria text containing those tokens would still pass. Signal: underdetermined substring as semantic stand-in. See https://texarkanine.github.io/slobac/taxonomy/loose-text-oracle/.
- **Prescribed remediation:** Assert a stable aria-label contract string (exact or structured attribute equality) that encodes ratio semantics.
- **Why this isn't a false positive:** Not vacuous absence-only — there is a positive match, but it does not lock the intended meaning.

### `tests/test_dispatcher_cli.py`

- **Location:** `tests/test_dispatcher_cli.py` :: `test_top_level_help_lists_all_subcommands` (line 58)
- **Smell:** `naming-lies`
- **Rationale:** Docstring claims help lists 'all five subcommands' but `SUBCOMMANDS` has ten entries and the body asserts all ten. Title/doc over-promises a stale count. Signal: title/docstring claim mismatches body. See https://texarkanine.github.io/slobac/taxonomy/naming-lies/.
- **Prescribed remediation:** Rename/fix the docstring to 'all registered subcommands' (or list the actual count); body is already correct.
- **Why this isn't a false positive:** Not under-specified naming — it asserts a specific false quantity ('five') that contradicts the fixture tuple.

### `tests/test_doctor.py`

- **Location:** `tests/test_doctor.py` :: `test_probe_reports_os_and_arch` (line 110)
- **Smell:** `deliverable-fossils`
- **Rationale:** Docstring leads with checklist id `B1:` rather than a durable product claim; Signal: docstrings citing AC/checklist identifiers. Body verifies doctor probe/smoke behavior, but naming reflects a gone plan checklist. See https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/.
- **Prescribed remediation:** Phase A rename: strip `B1` from the docstring and keep a behavior-shaped claim (e.g. what fact/exit/remedy is verified). Do not regroup yet (Phase B is cross-suite).
- **Why this isn't a false positive:** Not domain vocabulary — `B#` appears only in test docs/section headers, not in the doctor SUT API.

- **Location:** `tests/test_doctor.py` :: `test_probe_reports_gpu_and_driver_when_smi_parseable` (line 119)
- **Smell:** `deliverable-fossils`
- **Rationale:** Docstring leads with checklist id `B2:` rather than a durable product claim; Signal: docstrings citing AC/checklist identifiers. Body verifies doctor probe/smoke behavior, but naming reflects a gone plan checklist. See https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/.
- **Prescribed remediation:** Phase A rename: strip `B2` from the docstring and keep a behavior-shaped claim (e.g. what fact/exit/remedy is verified). Do not regroup yet (Phase B is cross-suite).
- **Why this isn't a false positive:** Not domain vocabulary — `B#` appears only in test docs/section headers, not in the doctor SUT API.

- **Location:** `tests/test_doctor.py` :: `test_probe_reports_gpu_none_when_smi_absent` (line 129)
- **Smell:** `deliverable-fossils`
- **Rationale:** Docstring leads with checklist id `B3:` rather than a durable product claim; Signal: docstrings citing AC/checklist identifiers. Body verifies doctor probe/smoke behavior, but naming reflects a gone plan checklist. See https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/.
- **Prescribed remediation:** Phase A rename: strip `B3` from the docstring and keep a behavior-shaped claim (e.g. what fact/exit/remedy is verified). Do not regroup yet (Phase B is cross-suite).
- **Why this isn't a false positive:** Not domain vocabulary — `B#` appears only in test docs/section headers, not in the doctor SUT API.

- **Location:** `tests/test_doctor.py` :: `test_probe_degrades_gracefully_on_smi_failure` (line 138)
- **Smell:** `deliverable-fossils`
- **Rationale:** Docstring leads with checklist id `B4:` rather than a durable product claim; Signal: docstrings citing AC/checklist identifiers. Body verifies doctor probe/smoke behavior, but naming reflects a gone plan checklist. See https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/.
- **Prescribed remediation:** Phase A rename: strip `B4` from the docstring and keep a behavior-shaped claim (e.g. what fact/exit/remedy is verified). Do not regroup yet (Phase B is cross-suite).
- **Why this isn't a false positive:** Not domain vocabulary — `B#` appears only in test docs/section headers, not in the doctor SUT API.

- **Location:** `tests/test_doctor.py` :: `test_probe_reports_torch_not_installed` (line 145)
- **Smell:** `deliverable-fossils`
- **Rationale:** Docstring leads with checklist id `B5:` rather than a durable product claim; Signal: docstrings citing AC/checklist identifiers. Body verifies doctor probe/smoke behavior, but naming reflects a gone plan checklist. See https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/.
- **Prescribed remediation:** Phase A rename: strip `B5` from the docstring and keep a behavior-shaped claim (e.g. what fact/exit/remedy is verified). Do not regroup yet (Phase B is cross-suite).
- **Why this isn't a false positive:** Not domain vocabulary — `B#` appears only in test docs/section headers, not in the doctor SUT API.

- **Location:** `tests/test_doctor.py` :: `test_probe_reports_torch_version_and_cuda` (line 152)
- **Smell:** `deliverable-fossils`
- **Rationale:** Docstring leads with checklist id `B6:` rather than a durable product claim; Signal: docstrings citing AC/checklist identifiers. Body verifies doctor probe/smoke behavior, but naming reflects a gone plan checklist. See https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/.
- **Prescribed remediation:** Phase A rename: strip `B6` from the docstring and keep a behavior-shaped claim (e.g. what fact/exit/remedy is verified). Do not regroup yet (Phase B is cross-suite).
- **Why this isn't a false positive:** Not domain vocabulary — `B#` appears only in test docs/section headers, not in the doctor SUT API.

- **Location:** `tests/test_doctor.py` :: `test_probe_never_imports_torch_eagerly` (line 190)
- **Smell:** `conditional-logic`
- **Rationale:** Body gates the isolation assertion with `if not already_loaded: assert "torch" not in sys.modules`, so one path never reaches that assertion (vacuous by omission). Signal: IfStatement whose consequent asserts and alternate is absent. See https://texarkanine.github.io/slobac/taxonomy/conditional-logic/.
- **Prescribed remediation:** Pin the precondition (e.g. ensure torch not preloaded via fixture/monkeypatch) and assert unconditionally, or split into two tests with explicit runner skip when torch is already loaded.
- **Why this isn't a false positive:** Not a runner-native skipif — silent `if` around an assert; pytest.skip is not used, so the weaker path still reports green.

- **Location:** `tests/test_doctor.py` :: `test_probe_never_imports_torch_eagerly` (line 190)
- **Smell:** `deliverable-fossils`
- **Rationale:** Docstring leads with checklist id `B7:` rather than a durable product claim; Signal: docstrings citing AC/checklist identifiers. Body verifies doctor probe/smoke behavior, but naming reflects a gone plan checklist. See https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/.
- **Prescribed remediation:** Phase A rename: strip `B7` from the docstring and keep a behavior-shaped claim (e.g. what fact/exit/remedy is verified). Do not regroup yet (Phase B is cross-suite).
- **Why this isn't a false positive:** Not domain vocabulary — `B#` appears only in test docs/section headers, not in the doctor SUT API.

- **Location:** `tests/test_doctor.py` :: `test_smoke_torch_missing_is_ratcheted_diagnosis` (line 230)
- **Smell:** `deliverable-fossils`
- **Rationale:** Docstring leads with checklist id `B8:` rather than a durable product claim; Signal: docstrings citing AC/checklist identifiers. Body verifies doctor probe/smoke behavior, but naming reflects a gone plan checklist. See https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/.
- **Prescribed remediation:** Phase A rename: strip `B8` from the docstring and keep a behavior-shaped claim (e.g. what fact/exit/remedy is verified). Do not regroup yet (Phase B is cross-suite).
- **Why this isn't a false positive:** Not domain vocabulary — `B#` appears only in test docs/section headers, not in the doctor SUT API.

- **Location:** `tests/test_doctor.py` :: `test_smoke_encoder_failure_names_error_and_next_action` (line 243)
- **Smell:** `deliverable-fossils`
- **Rationale:** Docstring leads with checklist id `B9:` rather than a durable product claim; Signal: docstrings citing AC/checklist identifiers. Body verifies doctor probe/smoke behavior, but naming reflects a gone plan checklist. See https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/.
- **Prescribed remediation:** Phase A rename: strip `B9` from the docstring and keep a behavior-shaped claim (e.g. what fact/exit/remedy is verified). Do not regroup yet (Phase B is cross-suite).
- **Why this isn't a false positive:** Not domain vocabulary — `B#` appears only in test docs/section headers, not in the doctor SUT API.

- **Location:** `tests/test_doctor.py` :: `test_smoke_encoder_construction_failure_is_caught` (line 257)
- **Smell:** `deliverable-fossils`
- **Rationale:** Docstring leads with checklist id `B9:` rather than a durable product claim; Signal: docstrings citing AC/checklist identifiers. Body verifies doctor probe/smoke behavior, but naming reflects a gone plan checklist. See https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/.
- **Prescribed remediation:** Phase A rename: strip `B9` from the docstring and keep a behavior-shaped claim (e.g. what fact/exit/remedy is verified). Do not regroup yet (Phase B is cross-suite).
- **Why this isn't a false positive:** Not domain vocabulary — `B#` appears only in test docs/section headers, not in the doctor SUT API.

- **Location:** `tests/test_doctor.py` :: `test_smoke_wrong_width_vector_is_a_failure` (line 274)
- **Smell:** `deliverable-fossils`
- **Rationale:** Docstring leads with checklist id `B10:` rather than a durable product claim; Signal: docstrings citing AC/checklist identifiers. Body verifies doctor probe/smoke behavior, but naming reflects a gone plan checklist. See https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/.
- **Prescribed remediation:** Phase A rename: strip `B10` from the docstring and keep a behavior-shaped claim (e.g. what fact/exit/remedy is verified). Do not regroup yet (Phase B is cross-suite).
- **Why this isn't a false positive:** Not domain vocabulary — `B#` appears only in test docs/section headers, not in the doctor SUT API.

- **Location:** `tests/test_doctor.py` :: `test_smoke_happy_path_reports_version_cuda_and_ok` (line 287)
- **Smell:** `deliverable-fossils`
- **Rationale:** Docstring leads with checklist id `B11:` rather than a durable product claim; Signal: docstrings citing AC/checklist identifiers. Body verifies doctor probe/smoke behavior, but naming reflects a gone plan checklist. See https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/.
- **Prescribed remediation:** Phase A rename: strip `B11` from the docstring and keep a behavior-shaped claim (e.g. what fact/exit/remedy is verified). Do not regroup yet (Phase B is cross-suite).
- **Why this isn't a false positive:** Not domain vocabulary — `B#` appears only in test docs/section headers, not in the doctor SUT API.

- **Location:** `tests/test_doctor.py` :: `test_smoke_real_model_end_to_end` (line 301)
- **Smell:** `deliverable-fossils`
- **Rationale:** Docstring leads with checklist id `B12:` rather than a durable product claim; Signal: docstrings citing AC/checklist identifiers. Body verifies doctor probe/smoke behavior, but naming reflects a gone plan checklist. See https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/.
- **Prescribed remediation:** Phase A rename: strip `B12` from the docstring and keep a behavior-shaped claim (e.g. what fact/exit/remedy is verified). Do not regroup yet (Phase B is cross-suite).
- **Why this isn't a false positive:** Not domain vocabulary — `B#` appears only in test docs/section headers, not in the doctor SUT API.

### `tests/test_doctor_cli.py`

- **Location:** `tests/test_doctor_cli.py` :: `test_probe_exits_zero_and_prints_fact_keys` (line 39)
- **Smell:** `deliverable-fossils`
- **Rationale:** Docstring is checklist-shaped (`B13: …`) instead of a product behavior title. Signal: AC/checklist identifiers in docstrings. See https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/.
- **Prescribed remediation:** Phase A: remove `B13` and rename the claim to the CLI behavior under test (exit code + printed keys/remedy/help).
- **Why this isn't a false positive:** Not product vocabulary — B-numbers are plan checklist labels, not doctor CLI domain terms.

- **Location:** `tests/test_doctor_cli.py` :: `test_smoke_torch_free_env_fails_loudly_with_remedy` (line 56)
- **Smell:** `deliverable-fossils`
- **Rationale:** Docstring is checklist-shaped (`B14: …`) instead of a product behavior title. Signal: AC/checklist identifiers in docstrings. See https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/.
- **Prescribed remediation:** Phase A: remove `B14` and rename the claim to the CLI behavior under test (exit code + printed keys/remedy/help).
- **Why this isn't a false positive:** Not product vocabulary — B-numbers are plan checklist labels, not doctor CLI domain terms.

- **Location:** `tests/test_doctor_cli.py` :: `test_help_documents_both_actions` (line 69)
- **Smell:** `deliverable-fossils`
- **Rationale:** Docstring is checklist-shaped (`B15: …`) instead of a product behavior title. Signal: AC/checklist identifiers in docstrings. See https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/.
- **Prescribed remediation:** Phase A: remove `B15` and rename the claim to the CLI behavior under test (exit code + printed keys/remedy/help).
- **Why this isn't a false positive:** Not product vocabulary — B-numbers are plan checklist labels, not doctor CLI domain terms.

### `tests/test_engine_env.py`

- **Location:** `tests/test_engine_env.py` :: `test_noop_when_inexact_check_passes` (line 53)
- **Smell:** `deliverable-fossils`
- **Rationale:** Docstring opens with deliverable id `B1:` from the engine-env checklist rather than naming the ensure/heal contract. Signal: checklist identifiers in docstrings. See https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/.
- **Prescribed remediation:** Phase A: strip `B1` and keep a behavior sentence about uv sync/--inexact/torch ensure outcomes.
- **Why this isn't a false positive:** Not SUT domain terms — B/T ids only appear in test documentation, not in engine_env exports.

- **Location:** `tests/test_engine_env.py` :: `test_heals_with_inexact_sync_when_check_fails` (line 71)
- **Smell:** `deliverable-fossils`
- **Rationale:** Docstring opens with deliverable id `B2:` from the engine-env checklist rather than naming the ensure/heal contract. Signal: checklist identifiers in docstrings. See https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/.
- **Prescribed remediation:** Phase A: strip `B2` and keep a behavior sentence about uv sync/--inexact/torch ensure outcomes.
- **Why this isn't a false positive:** Not SUT domain terms — B/T ids only appear in test documentation, not in engine_env exports.

- **Location:** `tests/test_engine_env.py` :: `test_heal_command_never_omits_inexact` (line 92)
- **Smell:** `deliverable-fossils`
- **Rationale:** Docstring opens with deliverable id `B3:` from the engine-env checklist rather than naming the ensure/heal contract. Signal: checklist identifiers in docstrings. See https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/.
- **Prescribed remediation:** Phase A: strip `B3` and keep a behavior sentence about uv sync/--inexact/torch ensure outcomes.
- **Why this isn't a false positive:** Not SUT domain terms — B/T ids only appear in test documentation, not in engine_env exports.

- **Location:** `tests/test_engine_env.py` :: `test_ensure_invokes_torch_ensure_after_deps` (line 104)
- **Smell:** `deliverable-fossils`
- **Rationale:** Docstring opens with deliverable id `T5:` from the engine-env checklist rather than naming the ensure/heal contract. Signal: checklist identifiers in docstrings. See https://texarkanine.github.io/slobac/taxonomy/deliverable-fossils/.
- **Prescribed remediation:** Phase A: strip `T5` and keep a behavior sentence about uv sync/--inexact/torch ensure outcomes.
- **Why this isn't a false positive:** Not SUT domain terms — B/T ids only appear in test documentation, not in engine_env exports.

### `tests/test_ingest_claude.py`

- **Location:** `tests/test_ingest_claude.py` :: `test_parse_ts_z_suffix_is_naive_utc` (line 22)
- **Smell:** `implementation-coupled`
- **Rationale:** Invokes `claude._parse_ts` (module-private) as the SUT. Signal: underscore-prefixed private helper called from tests. See https://texarkanine.github.io/slobac/taxonomy/implementation-coupled/.
- **Prescribed remediation:** Cover timestamp normalization via public `parse_session`/`ingest` outputs, or extract a public pure helper if timestamp parsing is a published contract.
- **Why this isn't a false positive:** Python `_` privacy is conventional; cross-module test calls still couple to an undocumented private API rather than the parser's public surface.

- **Location:** `tests/test_ingest_claude.py` :: `test_parse_ts_offset_converts_to_naive_utc` (line 29)
- **Smell:** `implementation-coupled`
- **Rationale:** Invokes `claude._parse_ts` (module-private) as the SUT. Signal: underscore-prefixed private helper called from tests. See https://texarkanine.github.io/slobac/taxonomy/implementation-coupled/.
- **Prescribed remediation:** Cover timestamp normalization via public `parse_session`/`ingest` outputs, or extract a public pure helper if timestamp parsing is a published contract.
- **Why this isn't a false positive:** Python `_` privacy is conventional; cross-module test calls still couple to an undocumented private API rather than the parser's public surface.

- **Location:** `tests/test_ingest_claude.py` :: `test_parse_ts_rejects_non_strings` (line 36)
- **Smell:** `implementation-coupled`
- **Rationale:** Invokes `claude._parse_ts` (module-private) as the SUT. Signal: underscore-prefixed private helper called from tests. See https://texarkanine.github.io/slobac/taxonomy/implementation-coupled/.
- **Prescribed remediation:** Cover timestamp normalization via public `parse_session`/`ingest` outputs, or extract a public pure helper if timestamp parsing is a published contract.
- **Why this isn't a false positive:** Python `_` privacy is conventional; cross-module test calls still couple to an undocumented private API rather than the parser's public surface.

### `tests/test_ingest_orchestrator.py`

- **Location:** `tests/test_ingest_orchestrator.py` :: `test_on_progress_none_emits_nothing` (line 104)
- **Smell:** `naming-lies`
- **Rationale:** Name claims `on_progress=None` emits nothing, but the body only asserts a successful Claude ingest (`sessions > 0`) and never observes callback emissions. Signal: title promises X, body verifies weaker Y. See https://texarkanine.github.io/slobac/taxonomy/naming-lies/.
- **Prescribed remediation:** Rename to match the body (e.g. completes without an on_progress callback) or strengthen by installing a callback spy and asserting zero calls.
- **Why this isn't a false positive:** Not synonymy — 'emits nothing' is a specific negative claim the assertions never check.

No findings for scope `tautology-theatre`.

No findings for scope `over-specified-mock`.

No findings for scope `prose-pin`.

No findings for scope `pseudo-tested`.

No findings for scope `vacuous-assertion`.

No findings for scope `monolithic-test-file`.

No findings for scope `shared-state`.

No findings for scope `mystery-guest`.

No findings for scope `rotten-green`.

## Behavior Summaries

| File | Line | Test ID | Behavior | Tier | Smells Found |
|------|------|---------|----------|------|--------------|
| tests-js/dashboard-core.test.mjs | 57 | sorts arbitrary harnesses and cycles stable positional colors | Sorts arbitrary harnesses and cycles stable positional colors SUT: sortedHarnesses, harnessColors. Asserts: assert.deepEqual(sortedHarnesses(raw), [ "alpha", "beta", "delta", "eta", "gamma", "iota", "…; assert.deepEqual(harnessColors(raw), { alpha: COLORS[0], beta: COLORS[1], delta: COLORS[2], … | unit | — |
| tests-js/dashboard-core.test.mjs | 83 | derives generic display labels from raw harness keys | Derives generic display labels from raw harness keys SUT: displayHarness. Asserts: assert.equal(displayHarness("claude-code"), "Claude Code"); assert.equal(displayHarness("cursor_cli"), "Cursor Cli"); assert.equal(displayHarness(" future--harness "), "Future Harness"); | unit | — |
| tests-js/dashboard-core.test.mjs | 90 | aggregates selected aligned series without mutating inputs | Aggregates selected aligned series without mutating inputs SUT: structuredClone, sumAligned. Asserts: assert.deepEqual(sumAligned(source, selected, 4), [4, 6, 0, 0]); assert.deepEqual(source, before); | unit | — |
| tests-js/dashboard-core.test.mjs | 101 | computes weighted averages with zero-observation guards | Computes weighted averages with zero-observation guards SUT: weightedSeries. Asserts: assert.deepEqual(weightedSeries(averages, counts, selected), [5.5, 0]); | unit | — |
| tests-js/dashboard-core.test.mjs | 107 | derives mode-independent overview cards and selected deltas | Derives mode-independent overview cards and selected deltas SUT: deriveOverviewCards. Asserts: assert.deepEqual(deriveOverviewCards(overview, selected), [ { key: "sessions", label: "Sessi…; assert.equal(deriveOverviewCards({ per_harness: {} }, [])[3].value, 0); | unit | — |
| tests-js/dashboard-core.test.mjs | 159 | projects delta uses prev_distinct_projects not summed prev_projects | Projects delta uses prev_distinct_projects not summed prev_projects SUT: deriveOverviewCards. Asserts: assert.ok(prevSum > overview.prev_distinct_projects); assert.deepEqual(projects, { key: "projects", label: "Projects", value: 2, delta: { text: "N… | unit | — |
| tests-js/dashboard-core.test.mjs | 197 | missing prev_distinct_projects degrades through formatDelta guards | Missing prev_distinct_projects degrades through formatDelta guards SUT: deriveOverviewCards. Asserts: assert.deepEqual(projects.delta, { text: "New", trend: "up" }); assert.deepEqual( deriveOverviewCards({ distinct_projects: 0, prev_distinct_projects: null }… | unit | — |
| tests-js/dashboard-core.test.mjs | 230 | derives per-harness KPI breakdown proportions | Derives per-harness KPI breakdown proportions SUT: deriveHarnessBreakdown. Asserts: assert.deepEqual(deriveHarnessBreakdown(overview, selected, "sessions"), [ { harness: "claud…; assert.deepEqual(deriveHarnessBreakdown(overview, selected, "average"), [ { harness: "claude…; assert.deepEqual( deriveHarnessBreakdown({ per_harness: { cursor: {} } }, ["cursor"… | unit | — |
| tests-js/dashboard-core.test.mjs | 261 | formats percentage deltas including new and neutral values | Formats percentage deltas including new and neutral values SUT: formatDelta. Asserts: assert.deepEqual(formatDelta(125, 100), { text: "+25%", trend: "up" }); assert.deepEqual(formatDelta(80, 100), { text: "-20%", trend: "down" }); assert.deepEqual(formatDelta(2, 0), { text: "New", trend: "up" }); | unit | — |
| tests-js/dashboard-core.test.mjs | 269 | derives deterministic chart heights from label counts | Derives deterministic chart heights from label counts SUT: chartHeight. Asserts: assert.equal(chartHeight(0), 240); assert.equal(chartHeight(3), 240); assert.equal(chartHeight(10), 340); | unit | — |
| tests-js/dashboard-core.test.mjs | 276 | defaults discovered harnesses and rejects an empty selection | Defaults discovered harnesses and rejects an empty selection SUT: transitionViewState. Asserts: assert.deepEqual(discovered, { state: { harnesses: ["claude-code", "cursor"], selected: ["cl…; assert.deepEqual(refused, { state: { harnesses: ["cursor"], selected: ["cursor"], mode: "agg… | unit | — |
| tests-js/dashboard-core.test.mjs | 320 | distinguishes mode redraws from selection refetches | Distinguishes mode redraws from selection refetches SUT: transitionViewState. Asserts: assert.equal( transitionViewState(state, { type: "mode", mode: "compare" }).effect, "render"…; assert.equal( transitionViewState(state, { type: "toggle", harness: "cursor" }).effect, "ref…; assert.equal( transitionViewState(state, { type: "mode", mode: "aggregate" }).ef… | unit | — |
| tests-js/dashboard-core.test.mjs | 342 | date-range changes refetch and identical presets are no-ops | Date-range changes refetch and identical presets are no-ops SUT: transitionViewState, resolveWindowBounds. Asserts: assert.equal(changed.effect, "refetch"); assert.equal(changed.state.dateRange, "7d"); assert.deepEqual(changed.state.window, resolveWindowBounds("7d", now)); | unit | — |
| tests-js/dashboard-core.test.mjs | 387 | resolveWindowBounds omits default and computes ISO since/until for presets | ResolveWindowBounds omits default and computes ISO since/until for presets SUT: resolveWindowBounds. Asserts: assert.equal(resolveWindowBounds("default", now), null); assert.equal(resolveWindowBounds("nope", now), null); assert.equal(week.until, "2026-07-10T18:00:00.000Z"); | unit | — |
| tests-js/dashboard-core.test.mjs | 404 | panelRangeLabels keep per-panel defaults and share preset window copy | PanelRangeLabels keep per-panel defaults and share preset window copy SUT: panelRangeLabels. Asserts: assert.equal(defaults.overviewAria, "Thirty-day overview"); assert.equal(defaults.daily, "Last 14 days"); assert.equal(defaults.projects, "Last 30 days"); | unit | — |
| tests-js/dashboard-core.test.mjs | 440 | builds aggregate and compare daily activity models | Builds aggregate and compare daily activity models SUT: buildDailyPanel, assertDataset. Asserts: assert.equal(aggregate.kind, "bar"); assert.equal(aggregate.stacked, false); assert.equal(compare.stacked, true); | unit | — |
| tests-js/dashboard-core.test.mjs | 455 | builds aggregate and compare project models | Builds aggregate and compare project models SUT: buildProjectsPanel, assertDataset. Asserts: assert.equal(aggregate.indexAxis, "y"); assert.equal(compare.stacked, true); | unit | — |
| tests-js/dashboard-core.test.mjs | 468 | projectHoverTitle returns id only when display differs | ProjectHoverTitle returns id only when display differs SUT: projectHoverTitle. Asserts: assert.equal(projectHoverTitle("stockroom", "home-me-stockroom"), "home-me-stockroom"); assert.equal(projectHoverTitle("slug", "slug"), null); assert.equal(projectHoverTitle("slug", null), null); | unit | — |
| tests-js/dashboard-core.test.mjs | 475 | buildProjectsPanel prefers friendly labels and sets labelTitles | BuildProjectsPanel prefers friendly labels and sets labelTitles SUT: buildProjectsPanel, assertDataset. Asserts: assert.deepEqual(panel.labels, ["stockroom", "other-slug"]); assert.deepEqual(panel.labelTitles, ["home-me-stockroom", null]); | unit | — |
| tests-js/dashboard-core.test.mjs | 487 | buildProjectsPanel falls back to projects when labels missing | BuildProjectsPanel falls back to projects when labels missing SUT: buildProjectsPanel. Asserts: assert.deepEqual(panel.labels, ["only-id"]); assert.deepEqual(panel.labelTitles, [null]); | unit | — |
| tests-js/dashboard-core.test.mjs | 497 | tooltipTitleFromLabelTitles surfaces slug when present | TooltipTitleFromLabelTitles surfaces slug when present SUT: tooltipTitleFromLabelTitles. Asserts: assert.equal( tooltipTitleFromLabelTitles(["home-me-stockroom", null], 0, "stockroom"), "hom…; assert.equal(tooltipTitleFromLabelTitles(["home-me-stockroom", null], 1, "other"), "other"); assert.equal(tooltipTitleFromLabelTitles(undefined, 0, "friendly"), "f… | unit | — |
| tests-js/dashboard-core.test.mjs | 506 | PANEL_HELP documents efficiency and first-prompt buckets | PANEL_HELP documents efficiency and first-prompt buckets SUT: dashboard-core. Asserts: assert.match(PANEL_HELP.efficiency, /abandoned/i); assert.match(PANEL_HELP.efficiency, /short/i); assert.match(PANEL_HELP.efficiency, /medium/i); | unit | loose-text-oracle |
| tests-js/dashboard-core.test.mjs | 519 | togglePanelHelp opens one, re-toggles closed, and switches panels | TogglePanelHelp opens one, re-toggles closed, and switches panels SUT: togglePanelHelp, closePanelHelp. Asserts: assert.equal(togglePanelHelp(null, "efficiency"), "efficiency"); assert.equal(togglePanelHelp("efficiency", "efficiency"), null); assert.equal(togglePanelHelp("efficiency", "first-prompt"), "first-prompt"); | unit | — |
| tests-js/dashboard-core.test.mjs | 526 | buildWrappedPanel marathon exposes subtitleTitle when name differs from id | BuildWrappedPanel marathon exposes subtitleTitle when name differs from id SUT: buildWrappedPanel. Asserts: assert.equal(marathon.subtitleTitle, "home-me-stockroom"); assert.equal(same.subtitleTitle, null); | unit | — |
| tests-js/dashboard-core.test.mjs | 560 | builds aggregate doughnut and compare tool models | Builds aggregate doughnut and compare tool models SUT: buildToolsPanel, assertDataset. Asserts: assert.equal(aggregate.kind, "doughnut"); assert.equal(compare.kind, "bar"); assert.equal(compare.indexAxis, "y"); | unit | — |
| tests-js/dashboard-core.test.mjs | 574 | writeShare returns zero on zero denominator and finite ratios otherwise | WriteShare returns zero on zero denominator and finite ratios otherwise SUT: writeShare. Asserts: assert.equal(writeShare(0, 0), 0); assert.equal(writeShare(0, 4), 0); assert.equal(writeShare(3, 1), 0.75); | unit | — |
| tests-js/dashboard-core.test.mjs | 581 | builds aggregate write-share ratio line with zeros for idle buckets | Builds aggregate write-share ratio line with zeros for idle buckets SUT: buildWriteReadPanel, assertDataset. Asserts: assert.equal(panel.kind, "line"); assert.equal(panel.stacked, false); assert.equal(panel.empty, false); | unit | — |
| tests-js/dashboard-core.test.mjs | 597 | builds compare write-share ratio lines per harness | Builds compare write-share ratio lines per harness SUT: buildWriteReadPanel, assertDataset. Asserts: assert.equal(panel.datasets.length, 2); assert.equal(panel.empty, false); assert.equal(panel.yMax, 1); | unit | — |
| tests-js/dashboard-core.test.mjs | 614 | marks write-read panel empty when every bucket has no tool activity | Marks write-read panel empty when every bucket has no tool activity SUT: buildWriteReadPanel. Asserts: assert.equal(panel.empty, true); assert.deepEqual(panel.datasets[0].data, [0, 0]); | unit | — |
| tests-js/dashboard-core.test.mjs | 625 | builds aggregate and compare efficiency models | Builds aggregate and compare efficiency models SUT: buildEfficiencyPanel, assertDataset. Asserts: assert.equal(compare.stacked, true); | unit | — |
| tests-js/dashboard-core.test.mjs | 637 | builds all-model aggregate and compare models | Builds all-model aggregate and compare models SUT: from, fill, buildModelsPanel, assertDataset. Asserts: assert.equal(aggregate.labels.length, 10); assert.equal(aggregate.height, 340); assert.equal(compare.stacked, true); | unit | — |
| tests-js/dashboard-core.test.mjs | 654 | builds weighted aggregate and grouped compare first-prompt models | Builds weighted aggregate and grouped compare first-prompt models SUT: buildFirstPromptPanel, assertDataset. Asserts: assert.equal(aggregate.stacked, false); assert.equal(compare.stacked, false); | unit | — |
| tests-js/dashboard-core.test.mjs | 669 | builds exactly eight factual wrapped cells with nullable fallbacks | Builds exactly eight factual wrapped cells with nullable fallbacks SUT: buildWrappedPanel. Asserts: assert.equal(cells.length, 8); assert.deepEqual(cells.map((cell) => cell.label), [ "Total Sessions", "Total Messages", "Dis…; assert.equal(cells[3].value, "Claude Code"); | unit | — |
| tests-js/dashboard-core.test.mjs | 712 | formats wrapped span and streak dates with short local dates | Formats wrapped span and streak dates with short local dates SUT: format, buildWrappedPanel. Asserts: assert.equal(cells[0].subtitle, `5 days · ${start} – ${end}`); assert.equal(cells[4].subtitle, `${start} – ${streakEnd}`); | unit | — |
| tests-js/dashboard-core.test.mjs | 733 | summarizes aggregate panel measured content | Summarizes aggregate panel measured content SUT: summarizeChartPanel. Asserts: assert.equal( summarizeChartPanel("Sessions by project", "aggregate", model), "Sessions by p… | unit | — |
| tests-js/dashboard-core.test.mjs | 746 | summarizes compare panel per-harness measured content | Summarizes compare panel per-harness measured content SUT: summarizeChartPanel. Asserts: assert.equal( summarizeChartPanel("Sessions by project", "compare", model), "Sessions by pro… | unit | — |
| tests-js/dashboard-core.test.mjs | 762 | summarizes empty panel without inventing values | Summarizes empty panel without inventing values SUT: summarizeChartPanel. Asserts: assert.equal( summarizeChartPanel("Daily session activity", "aggregate", model), "Daily sess… | unit | — |
| tests-js/dashboard-core.test.mjs | 775 | summarizes write-share ratio panel including idle zeros | Summarizes write-share ratio panel including idle zeros SUT: summarizeChartPanel. Asserts: assert.equal( summarizeChartPanel("Weekly write share", "aggregate", model), "Weekly write s… | unit | — |
| tests-js/dashboard-core.test.mjs | 788 | summarizeChartPanel leaves the input model unchanged | SummarizeChartPanel leaves the input model unchanged SUT: structuredClone, summarizeChartPanel. Asserts: assert.deepEqual(model, before); | unit | — |
| tests-js/dashboard-core.test.mjs | 800 | keeps every pure transformation input unchanged | Keeps every pure transformation input unchanged SUT: structuredClone, buildProjectsPanel, deriveOverviewCards. Asserts: assert.deepEqual(payload, before); | unit | — |
| tests-js/dashboard-data.test.mjs | 35 | builds eight same-origin request URLs with correct filtering | Builds eight same-origin request URLs with correct filtering SUT: buildRequestPlan, at. Asserts: assert.deepEqual(plan.map((item) => item.name), endpointNames); assert.match(item.url, /^\/api\//); assert.match(item.url, /harness=claude%2Fcli&harness=cursor%20pro/); | unit | — |
| tests-js/dashboard-data.test.mjs | 49 | fetches and names one complete parallel snapshot | Fetches and names one complete parallel snapshot SUT: fetchSnapshot, reverse, keys. Asserts: assert.equal(pending.length, 8); assert.deepEqual( pending.map((item) => item.url.split("?")[0]), endpointNames.map((name) =>…; assert.deepEqual(Object.keys(snapshot), endpointNames); | unit | — |
| tests-js/dashboard-data.test.mjs | 71 | rejects atomically with sanitized actionable API errors | Rejects atomically with sanitized actionable API errors SUT: fetchSnapshot. Asserts: assert.ok(error instanceof DashboardRequestError); assert.equal(error.message, "warehouse schema is behind"); assert.equal(error.action, "run `stockroom migrate`"); | unit | — |
| tests-js/dashboard-data.test.mjs | 114 | forwards abort signals to every request | Forwards abort signals to every request SUT: fetchSnapshot. Asserts: assert.equal(signals.length, 8); assert.ok(signals.every((signal) => signal === controller.signal)); | unit | — |
| tests-js/dashboard-data.test.mjs | 129 | allows only the latest request generation to commit | Allows only the latest request generation to commit SUT: createRequestGate, begin, commit, isCurrent. Asserts: assert.equal(first.signal.aborted, true); assert.equal(second.signal.aborted, false); assert.equal(first.commit(() => committed.push("first")), false); | unit | — |
| tests-js/dashboard-data.test.mjs | 143 | null or omitted window bounds match today's unwindowed URLs | Null or omitted window bounds match today's unwindowed URLs SUT: buildRequestPlan. Asserts: assert.deepEqual(buildRequestPlan(["cursor"], null), baseline); assert.deepEqual(buildRequestPlan(["cursor"], undefined), baseline); assert.deepEqual(buildRequestPlan(["cursor"], {}), baseline); | unit | — |
| tests-js/dashboard-data.test.mjs | 150 | window bounds append encoded since and until to all endpoints except wrapped | Window bounds append encoded since and until to all endpoints except wrapped SUT: buildRequestPlan. Asserts: assert.equal(item.url, "/api/wrapped"); assert.doesNotMatch(item.url, /since=\|until=/); assert.match(item.url, new RegExp(`since=${encodedSince}`)); | unit | — |
| tests-js/dashboard-data.test.mjs | 172 | fetchSnapshot forwards options.window into every non-wrapped pending URL | FetchSnapshot forwards options.window into every non-wrapped pending URL SUT: fetchSnapshot. Asserts: assert.equal(pending.length, 8); assert.equal(item.url, "/api/wrapped"); assert.doesNotMatch(item.url, /since=\|until=/); | unit | — |
| tests-js/dashboard-session.test.mjs | 16 | buildSessionViewSearchParams encodes the canonical session view | BuildSessionViewSearchParams encodes the canonical session view SUT: buildSessionViewSearchParams, get, toString. Asserts: assert.equal(params.get("view"), "session"); assert.equal(params.get("harness"), "cursor"); assert.equal(params.get("session"), "abc/def+1"); | unit | — |
| tests-js/dashboard-session.test.mjs | 27 | parseSessionViewParams requires view=session plus both ids | ParseSessionViewParams requires view=session plus both ids SUT: parseSessionViewParams. Asserts: assert.deepEqual( parseSessionViewParams( new URLSearchParams("view=session&harness=claude&s…; assert.equal( parseSessionViewParams(new URLSearchParams("harness=claude&session=s1")), null…; assert.equal( parseSessionViewParams(new URLSearchParams("view=session… | unit | — |
| tests-js/dashboard-session.test.mjs | 49 | buildSessionDeepLink appends encoded query to a base URL | BuildSessionDeepLink appends encoded query to a base URL SUT: buildSessionDeepLink. Asserts: assert.equal( buildSessionDeepLink("http://127.0.0.1:58008/", "cursor", "s1"), "http://127.0…; assert.equal( buildSessionDeepLink("http://127.0.0.1:58008", "cursor", "a/b"), "http://127.0… | unit | — |
| tests-js/dashboard-session.test.mjs | 60 | formatSessionMarkdownExport builds title, turns, and fenced tools | FormatSessionMarkdownExport builds title, turns, and fenced tools SUT: formatSessionMarkdownExport. Asserts: assert.match(markdown, /^# cursor \/ s1/); assert.match(markdown, /project: stockroom/); assert.match(markdown, /## user\n\nhello \*\*world\*\*/); | unit | — |
| tests-js/dashboard-session.test.mjs | 90 | formatSessionJsonExport pretty-prints the detail payload identity | FormatSessionJsonExport pretty-prints the detail payload identity SUT: formatSessionJsonExport, stringify. Asserts: assert.equal(formatSessionJsonExport(detail), `${JSON.stringify(detail, null, 2)}\n`); | unit | — |
| tests-js/dashboard-session.test.mjs | 99 | ansiToHtml renders bold SGR and escapes HTML | AnsiToHtml renders bold SGR and escapes HTML SUT: ansiToHtml. Asserts: assert.match(html, /&lt;local-command-stdout&gt;/); assert.match(html, /<strong>Sonnet 5<\/strong>/); assert.doesNotMatch(html, /\u001b/); | unit | — |
| tests-js/dashboard-session.test.mjs | 109 | ansiToHtml supports colors reset and newlines | AnsiToHtml supports colors reset and newlines SUT: ansiToHtml. Asserts: assert.match(html, /style="color:#c00"/); assert.match(html, /red/); assert.match(html, /<br>/); | unit | — |
| tests-js/dashboard-session.test.mjs | 117 | renderSessionMessageHtml uses ANSI path when CSI present else markdown | RenderSessionMessageHtml uses ANSI path when CSI present else markdown SUT: renderSessionMessageHtml. Asserts: assert.equal( renderSessionMessageHtml("hello **x**", md), "<p>hello **x**</p>", ); assert.match(ansiHtml, /<strong>b<\/strong>/); assert.doesNotMatch(ansiHtml, /<p>/); | unit | — |
| tests-js/dashboard-session.test.mjs | 128 | isActiveSessionView requires matching harness and session id | IsActiveSessionView requires matching harness and session id SUT: isActiveSessionView. Asserts: assert.equal(isActiveSessionView({ harness: "c", sessionId: "1" }, "c", "1"), true); assert.equal(isActiveSessionView({ harness: "c", sessionId: "1" }, "c", "2"), false); assert.equal(isActiveSessionView(null, "c", "1"), false); | unit | — |
| tests-js/dashboard-session.test.mjs | 134 | shouldUseHistoryBackForSessionClose only when history state is session | ShouldUseHistoryBackForSessionClose only when history state is session SUT: shouldUseHistoryBackForSessionClose. Asserts: assert.equal(shouldUseHistoryBackForSessionClose({ view: "session" }), true); assert.equal(shouldUseHistoryBackForSessionClose({ view: "metrics" }), false); assert.equal(shouldUseHistoryBackForSessionClose(null), false); | unit | — |
| tests-js/dashboard-time.test.mjs | 9 | parseDisplayDate treats naive ISO datetimes as UTC | ParseDisplayDate treats naive ISO datetimes as UTC SUT: parseDisplayDate. Asserts: assert.ok(parsed); assert.equal(parsed.getTime(), Date.parse("2026-07-10T03:22:00Z")); | unit | — |
| tests-js/dashboard-time.test.mjs | 15 | parseDisplayDate honors explicit Z and offsets | ParseDisplayDate honors explicit Z and offsets SUT: parseDisplayDate. Asserts: assert.equal( parseDisplayDate("2026-07-10T03:22:00Z").getTime(), Date.parse("2026-07-10T03:…; assert.equal( parseDisplayDate("2026-07-09T22:22:00-05:00").getTime(), Date.parse("2026-07-1… | unit | — |
| tests-js/dashboard-time.test.mjs | 26 | parseDisplayDate keeps date-only labels as local calendar midnight | ParseDisplayDate keeps date-only labels as local calendar midnight SUT: parseDisplayDate. Asserts: assert.ok(parsed); assert.equal(parsed.getTime(), new Date("2026-07-10T00:00:00").getTime()); | unit | — |
| tests-js/dashboard-time.test.mjs | 32 | formatDate returns em dash for empty values | FormatDate returns em dash for empty values SUT: formatDate. Asserts: assert.equal(formatDate(""), "—"); assert.equal(formatDate(null), "—"); | unit | — |
| tests/test_dashboard_cli.py | 14 | test_default_port_is_58008 | Default dashboard listener port is 58008. SUT: dashboard_cli.DEFAULT_PORT. Asserts: assert dashboard_cli.DEFAULT_PORT == 58008 | unit | — |
| tests/test_dashboard_cli.py | 19 | test_already_serving_prints_url_without_spawn | A successful probe with no usable identity leaves the listener alone. SUT: dashboard_cli.main. Asserts: assert result == 0; assert spawned == []; assert killed == [] | unit | — |
| tests/test_dashboard_cli.py | 36 | test_same_identity_reuses_without_kill_or_spawn | Matching owned identity is an idempotent no-op. SUT: dashboard_cli.main. Asserts: assert result == 0; assert spawned == []; assert killed == [] | unit | — |
| tests/test_dashboard_cli.py | 60 | test_stale_owned_identity_kills_waits_and_spawns | Stale owned listener is replaced from the current engine. SUT: dashboard_cli.main. Asserts: assert result == 0; assert killed == [55]; assert waits == [58008] | unit | — |
| tests/test_dashboard_cli.py | 96 | test_unverified_identity_is_left_alone | Identity pid that fails ownership verify is never killed. SUT: dashboard_cli.main. Asserts: assert result == 0; assert killed == []; assert spawned == [] | unit | — |
| tests/test_dashboard_cli.py | 120 | test_kill_failure_still_exits_zero | Replace path degrades to success when kill raises. SUT: dashboard_cli.main. Asserts: assert result == 0; assert spawned == []; assert capsys.readouterr().out == "http://127.0.0.1:58008/\n" | unit | — |
| tests/test_dashboard_cli.py | 146 | test_version_mismatch_same_app_dir_replaces | Same app_dir with a different version is treated as stale. SUT: dashboard_cli.main. Asserts: assert result == 0; assert killed == [55]; assert len(spawned) == 1 | unit | — |
| tests/test_dashboard_cli.py | 171 | test_free_port_spawns_foreground_reexec_and_respects_port | A free port detaches the exact foreground module re-exec. SUT: dashboard_cli.main. Asserts: assert result == 0; assert spawned == [; assert capsys.readouterr().out == "http://127.0.0.1:7777/\n" | unit | — |
| tests/test_dashboard_cli.py | 193 | test_foreground_serves_in_process_without_probe_or_spawn | Foreground mode binds and serves directly through the server seam. SUT: dashboard_cli.main. Asserts: assert result == 0; assert events == [("bound", 8888), "served", "closed"]; assert len(written) == 1 | unit | — |
| tests/test_dashboard_cli.py | 227 | test_foreground_bind_writes_identity | Successful foreground bind persists pid/app_dir/version/port identity. SUT: dashboard_cli.main, dash_identity.read. Asserts: assert result == 0; assert record is not None; assert record.pid == os.getpid() | unit | — |
| tests/test_dashboard_cli.py | 252 | test_foreground_bind_race_is_success_elsewhere | EADDRINUSE means another child won the bind mutex and is success. SUT: dashboard_cli.main. Asserts: assert result == 0; assert capsys.readouterr().out == "http://127.0.0.1:58008/\n" | unit | — |
| tests/test_dashboard_cli.py | 263 | test_foreground_startup_does_not_require_a_warehouse | Missing warehouse is a per-request 503, never a launcher precondition. SUT: dashboard_cli.main. Asserts: assert not warehouse_home.exists(); assert result == 0; assert events == ["served", "closed"] | unit | — |
| tests/test_dashboard_identity.py | 9 | test_identity_round_trip_under_stockroom_home | Write then read returns the same pid, app_dir, version, and port. SUT: dash_identity.DashboardIdentity, dash_identity.write, dash_identity.read. Asserts: assert written.is_file(); assert written.parent == warehouse_home; assert dash_identity.read(58008) == record | unit | — |
| tests/test_dashboard_identity.py | 23 | test_identity_missing_file_reads_as_none | Absent identity file yields None. SUT: dash_identity.read. Asserts: assert dash_identity.read(58008) is None | unit | — |
| tests/test_dashboard_identity.py | 28 | test_identity_corrupt_file_reads_as_none | Unparseable identity content yields None. SUT: dash_identity.path, dash_identity.read. Asserts: assert dash_identity.read(58008) is None | unit | — |
| tests/test_dashboard_identity.py | 36 | test_identity_clear_removes_file | Clear() deletes the identity file when present. SUT: dash_identity.write, dash_identity.DashboardIdentity, dash_identity.path, dash_identity.clear. Asserts: assert dash_identity.path(7777).is_file(); assert not dash_identity.path(7777).exists(); assert dash_identity.read(7777) is None | unit | — |
| tests/test_dashboard_identity.py | 52 | test_current_app_dir_is_engine_root | Current_app_dir() resolves to the engine dir that contains src/stockroom. SUT: dash_identity.current_app_dir. Asserts: assert (app_dir / "src" / "stockroom" / "__init__.py").is_file(); assert Path(stockroom.__file__).resolve().is_relative_to(app_dir) | unit | — |
| tests/test_dashboard_metrics.py | 120 | test_parse_window_accepts_iso_dates_and_uses_exclusive_until | Dates parse at midnight and preserve the inclusive/exclusive contract. SUT: metrics.parse_window. Asserts: assert since == datetime(2026, 1, 10); assert until == datetime(2026, 1, 12, 12, 30) | unit | — |
| tests/test_dashboard_metrics.py | 129 | test_parse_window_applies_default_and_rejects_reversed_bounds | A missing since uses the default duration; reversed windows are invalid. SUT: metrics.parse_window. Asserts: assert since == datetime(2026, 1, 18, 12); assert until == now | unit | — |
| tests/test_dashboard_metrics.py | 139 | test_parse_window_default_now_is_utc | When ``now`` is omitted, the window end comes from ``utc_now``. SUT: metrics.parse_window. Asserts: assert until == frozen; assert since == datetime(2026, 7, 9, 3, 22, 0) | unit | — |
| tests/test_dashboard_metrics.py | 150 | test_iso_appends_z_for_naive_utc_datetimes | Dashboard datetime wire format marks naive UTC with a trailing ``Z``. SUT: metrics._iso. Asserts: assert metrics._iso(datetime(2026, 7, 10, 3, 22, 0)) == "2026-07-10T03:22:00Z"; assert metrics._iso(None) is None | unit | implementation-coupled |
| tests/test_dashboard_metrics.py | 156 | test_empty_overview_has_stable_zero_shape | A migrated but un-ingested warehouse returns a complete empty payload. SUT: metrics.overview. Asserts: assert metrics.overview( | unit | — |
| tests/test_dashboard_metrics.py | 172 | test_trends_defaults_to_fourteen_days_and_twelve_calendar_weeks | Default trend windows expose exactly 14 day and 12 week labels. SUT: metrics.trends. Asserts: assert result["daily"]["granularity"] == "day"; assert result["daily"]["labels"] == [; assert result["weekly"]["granularity"] == "week" | unit | — |
| tests/test_dashboard_metrics.py | 189 | test_trends_bounded_window_uses_shared_adaptive_granularity | Range picker windows share one bucket size across both time-series panels. SUT: metrics.trends. Asserts: assert seven_day["daily"]["granularity"] == "day"; assert seven_day["weekly"]["granularity"] == "day"; assert seven_day["daily"]["labels"] == seven_day["weekly"]["labels"] | unit | — |
| tests/test_dashboard_metrics.py | 259 | test_overview_returns_per_harness_counts_previous_window_and_rollups | Overview counts current/previous KPIs and distinct projects correctly. SUT: metrics.overview. Asserts: assert result == { | unit | — |
| tests/test_dashboard_metrics.py | 302 | test_overview_prev_distinct_projects_unions_shared_previous_projects | Previous distinct is a union; shared projects must not inflate the rollup. SUT: metrics.overview. Asserts: assert result["per_harness"]["cursor"]["prev_projects"] == 1; assert result["per_harness"]["claude"]["prev_projects"] == 1; assert prev_sum == 2 | unit | — |
| tests/test_dashboard_metrics.py | 329 | test_overview_filter_and_unknown_harness_are_mode_agnostic | Repeated selection narrows keys; unknown harnesses return a zero shape. SUT: metrics.overview. Asserts: assert list(selected["per_harness"]) == ["cursor"]; assert selected["distinct_projects"] == 1; assert selected["prev_distinct_projects"] == 1 | unit | — |
| tests/test_dashboard_metrics.py | 350 | test_trends_zero_fills_daily_and_classifies_weekly_tools | Daily series fill gaps; only configured tool names enter read/write. SUT: metrics.trends. Asserts: assert result == { | unit | — |
| tests/test_dashboard_metrics.py | 418 | test_window_edges_source_mtime_and_subagents_are_cross_cutting | Windows are [since, until), Cursor uses mtime, and subagents stay excluded. SUT: metrics.overview, metrics.trends, metrics.tools, metrics.efficiency. Asserts: assert overview["per_harness"]["cursor"]["sessions"] == 2; assert sum(trends["daily"]["sessions"]["cursor"]) == 2; assert sum(trends["weekly"]["reads"]["cursor"]) == 1 | unit | — |
| tests/test_dashboard_metrics.py | 486 | test_projects_ranks_by_selected_total_with_aligned_harness_counts | Projects rank deterministically and each harness gets aligned counts. SUT: metrics.projects. Asserts: assert result == {; assert selected == { | unit | — |
| tests/test_dashboard_metrics.py | 532 | test_projects_labels_use_unique_cwd_basename | When all non-NULL cwds share one leaf, labels use that basename. SUT: metrics.projects. Asserts: assert result["projects"] == ["home-me-stockroom"]; assert result["labels"] == ["stockroom"] | unit | — |
| tests/test_dashboard_metrics.py | 561 | test_projects_labels_fall_back_to_id_when_short_names_disagree | Disagreeing cwd basenames for one project_id → label is the full id. SUT: metrics.projects. Asserts: assert result["projects"] == ["ambiguous-slug"]; assert result["labels"] == ["ambiguous-slug"] | unit | — |
| tests/test_dashboard_metrics.py | 590 | test_projects_ranking_stays_by_id_when_basenames_collide | Same leaf across different project_ids still ranks and labels by id order. SUT: metrics.projects. Asserts: assert result["projects"] == ["path-a-stockroom", "path-b-stockroom"]; assert result["labels"] == ["stockroom", "stockroom"]; assert result["sessions"] == {"claude": [0, 1], "cursor": [2, 0]} | unit | — |
| tests/test_dashboard_metrics.py | 629 | test_project_display_name_uses_cwd_basename_or_id | Shared helper: basename when cwd present, else project_id. SUT: metrics.project_display_name. Asserts: assert metrics.project_display_name("/home/me/stockroom", "slug") == "stockroom"; assert metrics.project_display_name(None, "slug") == "slug"; assert metrics.project_display_name("", "slug") == "slug" | unit | — |
| tests/test_dashboard_metrics.py | 636 | test_tools_ranks_calls_with_per_harness_breakout | Tool names rank by total selected calls with deterministic ties. SUT: metrics.tools. Asserts: assert result == { | unit | — |
| tests/test_dashboard_metrics.py | 672 | test_models_unifies_message_and_session_grains_once_per_session | A model counts once per session across message and session-list grains. SUT: metrics.models. Asserts: assert result == { | unit | — |
| tests/test_dashboard_metrics.py | 730 | test_efficiency_covers_boundaries_and_weighted_prompt_averages | Message and prompt buckets include every documented boundary. SUT: metrics.efficiency. Asserts: assert result == { | unit | — |
| tests/test_dashboard_metrics.py | 772 | test_sessions_are_recent_filtered_and_display_truncated | Recent sessions are newest-first with model/project/prompt display fields. SUT: metrics.sessions. Asserts: assert result == [; assert [row["harness"] for row in metrics.sessions(migrated_con, ["cursor"])] == [ | unit | — |
| tests/test_dashboard_metrics.py | 846 | test_wrapped_returns_all_time_rollups_and_ignores_selector | Wrapped computes totals, streak, marathon, peak, and top tool all-time. SUT: metrics.wrapped. Asserts: assert result == { | unit | — |
| tests/test_dashboard_metrics.py | 911 | test_session_detail_reconstructs_ordered_messages_and_nested_tools | Detail returns full text, ordinal-ordered messages, and nested tool_calls. SUT: metrics.session_detail. Asserts: assert result is not None; assert result["harness"] == "cursor"; assert result["session_id"] == "detail-1" | unit | — |
| tests/test_dashboard_metrics.py | 982 | test_session_detail_missing_session_returns_none | Unknown (harness, session_id) yields None for the server to map to 404. SUT: metrics.session_detail. Asserts: assert metrics.session_detail(migrated_con, "cursor", "missing") is None | unit | — |
| tests/test_dashboard_metrics.py | 989 | test_session_detail_serves_subagent_when_addressed_directly | Subagent sessions are excluded from the list but available by detail id. SUT: metrics.session_detail, metrics.sessions. Asserts: assert result is not None; assert result["is_subagent"] is True; assert result["parent_session_id"] == "parent" | unit | — |
| tests/test_dashboard_server.py | 56 | test_every_api_route_returns_json_from_current_warehouse | The endpoint registry is fully routable over a current warehouse. SUT: warehouse.open, metrics.ENDPOINTS. Asserts: assert httpd.server_address[0] == "127.0.0.1"; assert status == 404; assert payload["error"] == "not found" | integration | — |
| tests/test_dashboard_server.py | 83 | test_unknown_api_and_bad_parameters_return_clean_client_errors | Unknown routes are 404; malformed windows and limits are 400 JSON. SUT: warehouse.open. Asserts: assert status == 404; assert payload["error"] == "not found"; assert status == 400 | integration | — |
| tests/test_dashboard_server.py | 104 | test_partial_bounds_preserve_endpoint_specific_defaults | Until-only trends keep 14d/12w; sessions remain open-ended recent-N. SUT: warehouse.open. Asserts: assert status == 200; assert len(trends["daily"]["labels"]) == 14; assert len(trends["weekly"]["labels"]) == 12 | integration | — |
| tests/test_dashboard_server.py | 143 | test_repeated_harness_limit_cap_and_short_timeout_are_wired | HTTP preserves repeated filters, clamps limit, and opens with 2s backoff. SUT: metrics.ENDPOINTS. Asserts: assert status == 200; assert payload["harnesses"] == ["cursor", "claude"]; assert payload["limit"] == 500 | integration | — |
| tests/test_dashboard_server.py | 180 | test_missing_stale_and_busy_warehouses_return_actionable_503 | Every expected warehouse refusal has stable error/action JSON. SUT: warehouse.warehouse_path, migrate.ensure_schema_version_table, migrate.SCHEMA_VERSION_TABLE, migrate.current_version. Asserts: assert status == 503; assert set(payload) == {"error", "action"}; assert "stockroom ingest" in payload["action"] | integration | — |
| tests/test_dashboard_server.py | 224 | test_static_root_and_traversal_guard | The root serves packaged HTML and encoded traversal cannot escape it. SUT: dashboard_server. Asserts: assert status == 200; assert content_type.startswith("text/html"); assert b"stockroom dashboard" in body | integration | — |
| tests/test_dashboard_server.py | 237 | test_dashboard_javascript_assets_have_browser_mime_types | Authored modules and vendored Chart.js are served as JavaScript. SUT: dashboard_server. Asserts: assert status == 200, asset; assert content_type.split(";", 1)[0] in {; assert body | integration | — |
| tests/test_dashboard_server.py | 259 | test_unexpected_exception_returns_clean_json_500 | Unexpected failures never leak tracebacks into the HTTP response. SUT: dashboard_server. Asserts: assert status == 500; assert payload == {"error": "internal server error"}; assert "private detail" not in json.dumps(payload) | integration | — |
| tests/test_dashboard_server.py | 275 | test_session_api_returns_detail_and_client_errors | GET /api/session requires harness+session; maps missing rows to 404. SUT: warehouse.open. Asserts: assert status == 200; assert payload["session_id"] == "api-sess"; assert payload["harness"] == "cursor" | integration | loose-text-oracle |
| tests/test_dashboard_server.py | 324 | test_ingest_to_server_integration | A real fixture ingest is visible through the overview endpoint. SUT: warehouse.open, ingest.ingest. Asserts: assert status == 200; assert payload["per_harness"]["cursor"]["sessions"] > 0; assert payload["per_harness"]["claude"]["sessions"] > 0 | integration | wrong-level? |
| tests/test_dashboard_static.py | 37 | test_dashboard_document_has_semantic_controls_panels_and_fallbacks | The packaged page exposes the complete accessible single-pane shell. SUT: dashboard static assets. Asserts: assert "<!doctype html>" in source.lower(); assert "stockroom dashboard" in " ".join(parser.text).lower(); assert "main" in tags | unit | — |
| tests/test_dashboard_static.py | 72 | test_dashboard_resources_are_local_and_loaded_in_dependency_order | Every resource is local; Chart.js and markdown-it load before the module. SUT: dashboard static assets. Asserts: assert references; assert all(; assert "http://" not in lowered | unit | — |
| tests/test_dashboard_static.py | 106 | test_dashboard_adapter_imports_authored_modules | The effects adapter imports core, data, and session helpers. SUT: dashboard static assets. Asserts: assert 'from "./dashboard-core.mjs"' in adapter; assert 'from "./dashboard-data.mjs"' in adapter; assert 'from "./dashboard-session.mjs"' in adapter | unit | — |
| tests/test_dashboard_static.py | 114 | test_session_pane_exposes_navigation_export_and_turn_landmarks | Session inspection pane has back, copy-link, export, and turns regions. SUT: dashboard static assets. Asserts: assert "metrics-pane" in by_id; assert "session-pane" in by_id; assert ( | unit | — |
| tests/test_dashboard_static.py | 142 | test_session_pane_toolbar_and_bubble_layout_contracts | Toolbar uses emoji affordances; turns are SMS-aligned with scrollable tools. SUT: dashboard static assets. Asserts: assert "⬅️ Back to metrics" in source; assert "🔗 Copy deep-link" in source; assert "📥 Export markdown" in source | unit | presentation-coupled |
| tests/test_dashboard_static.py | 168 | test_dashboard_top_controls_expose_date_range_and_segmented_mode | Date-range presets and Aggregate/Compare read as exclusive segmented controls. SUT: dashboard static assets. Asserts: assert by_id["date-range-selector"][0] == "fieldset"; assert by_id["mode-selector"][0] == "fieldset"; assert "segmented" in (by_id["date-range-selector"][1].get("class") or "").split() | unit | — |
| tests/test_dashboard_static.py | 209 | test_write_read_chart_aria_describes_ratio_not_absolute_volumes | Write/Read canvas fallback copy matches ratio semantics. SUT: dashboard static assets. Asserts: assert "ratio" in label or "share" in label; assert "tool calls chart" not in label | unit | loose-text-oracle |
| tests/test_dashboard_static.py | 221 | test_info_controls_only_on_efficiency_and_first_prompt_panels | Help chrome is limited to Session Efficiency and First-Prompt Quality. SUT: dashboard static assets. Asserts: assert len(info_buttons) == 2; assert all(btn.get("type") == "button" for btn in info_buttons); assert all(btn.get("aria-expanded") == "false" for btn in info_buttons) | unit | — |
| tests/test_dispatcher_cli.py | 58 | test_top_level_help_lists_all_subcommands | ``--help`` exits 0 and stdout names all five subcommands. SUT: CLI subprocess. Asserts: assert result.returncode == 0, result.stderr; assert sub in result.stdout | integration | naming-lies |
| tests/test_dispatcher_cli.py | 66 | test_version_flag_prints_package_version | ``--version`` exits 0 and prints ``stockroom.__version__``. SUT: CLI subprocess. Asserts: assert result.returncode == 0, result.stderr; assert stockroom.__version__ in result.stdout | integration | — |
| tests/test_dispatcher_cli.py | 73 | test_no_args_prints_usage_to_stderr | No arguments exits 2 with usage on stderr. SUT: CLI subprocess. Asserts: assert result.returncode == 2; assert "usage" in result.stderr.lower() | integration | — |
| tests/test_dispatcher_cli.py | 80 | test_unknown_subcommand_is_clean_error | An unknown token exits 2, names the token on stderr, no traceback. SUT: CLI subprocess. Asserts: assert result.returncode == 2; assert "bogus" in result.stderr; assert "Traceback" not in result.stderr | integration | — |
| tests/test_dispatcher_cli.py | 88 | test_help_forwards_to_each_module | ``<sub> --help`` exits 0 for every subcommand and shows help text that is identifiably the target module's own (a module-unique flag or phrase). SUT: CLI subprocess. Asserts: assert result.returncode == 0, f"{sub}: {result.stderr}"; assert fingerprints[sub] in result.stdout, f"{sub} help missing fingerprint" | integration | — |
| tests/test_dispatcher_cli.py | 110 | test_dispatch_runs_query_for_real | ``stockroom query "SELECT 1 AS n"`` against an ingested warehouse exits 0 with the same TSV output as ``python -m stockroom.query``. SUT: CLI subprocess. Asserts: assert ingested.returncode == 0, ingested.stderr; assert via_dispatcher.returncode == 0, via_dispatcher.stderr; assert direct.returncode == 0, direct.stderr | integration | — |
| tests/test_dispatcher_cli.py | 137 | test_exit_codes_propagate_from_module | The module's own exit codes pass through unchanged: empty SQL → 2; missing warehouse → 1 with the module's 'run ingest first' hint. SUT: CLI subprocess. Asserts: assert empty_sql.returncode == 2; assert no_warehouse.returncode == 1; assert "ingest" in no_warehouse.stderr.lower() | integration | — |
| tests/test_dispatcher_cli.py | 148 | test_dispatch_migrate_bootstraps_warehouse | ``stockroom migrate`` on a fresh home exits 0 and creates the warehouse file (locks the dispatch wiring end to end). SUT: warehouse.duckdb. Asserts: assert result.returncode == 0, result.stderr; assert (home / "warehouse.duckdb").is_file() | integration | — |
| tests/test_doctor.py | 110 | test_probe_reports_os_and_arch | Os/arch come from the platform module as stable facts. SUT: doctor. Asserts: assert facts["os"] == platform.system(); assert facts["arch"] == platform.machine() | unit | deliverable-fossils |
| tests/test_doctor.py | 119 | test_probe_reports_gpu_and_driver_when_smi_parseable | A healthy nvidia-smi yields GPU name, driver, driver CUDA ceiling, and the compute capability (the sm_ generation input). SUT: doctor. Asserts: assert facts["gpu"] == "NVIDIA GeForce GTX 1070"; assert facts["driver"] == "582.28"; assert facts["driver-cuda"] == "13.0" | unit | deliverable-fossils |
| tests/test_doctor.py | 129 | test_probe_reports_gpu_none_when_smi_absent | No nvidia-smi on PATH is a fact (gpu: none), never an error. SUT: doctor. Asserts: assert facts["gpu"] == "none"; assert "driver" not in facts; assert "driver-cuda" not in facts | unit | deliverable-fossils |
| tests/test_doctor.py | 138 | test_probe_degrades_gracefully_on_smi_failure | An erroring or garbage-emitting nvidia-smi degrades to an 'unavailable' fact — no traceback, no raise. SUT: doctor. Asserts: assert facts["gpu"] == "unavailable" | unit | deliverable-fossils |
| tests/test_doctor.py | 145 | test_probe_reports_torch_not_installed | An unimportable torch is a reported fact. SUT: doctor. Asserts: assert facts["torch"] == "not installed"; assert "torch-cuda" not in facts | unit | deliverable-fossils |
| tests/test_doctor.py | 152 | test_probe_reports_torch_version_and_cuda | An importable torch yields its version and CUDA availability. SUT: doctor. Asserts: assert facts["torch"] == "2.11.0"; assert facts["torch-cuda"] == "True" | unit | deliverable-fossils |
| tests/test_doctor.py | 159 | test_probe_reports_engine_dir | The engine dir fact names where the engine environment lives. SUT: doctor. Asserts: assert facts["engine-dir"] == str(default_app_dir()) | unit | — |
| tests/test_doctor.py | 165 | test_probe_reports_home_and_source_from_resolve_home | Probe reports ``home`` + ``home-source`` from ``warehouse.resolve_home``. SUT: warehouse.HOME_ENV_VAR, warehouse.XDG_DATA_HOME_ENV_VAR, warehouse.HOME_SOURCE_XDG. Asserts: assert facts["home"] == str(xdg / "stockroom"); assert facts["home-source"] == warehouse.HOME_SOURCE_XDG | unit | — |
| tests/test_doctor.py | 177 | test_probe_home_facts_do_not_mkdir | Doctor probe must not create the resolved home directory. SUT: warehouse.HOME_ENV_VAR, warehouse.XDG_DATA_HOME_ENV_VAR. Asserts: assert facts["home"] == str(target); assert not target.exists() | unit | — |
| tests/test_doctor.py | 190 | test_probe_never_imports_torch_eagerly | Importing the module and probing goes through the injected importer only — the real torch is never touched. SUT: doctor. Asserts: assert calls == ["torch"]; assert facts["torch"] == "not installed"; assert "torch" not in sys.modules | unit | conditional-logic, deliverable-fossils |
| tests/test_doctor.py | 207 | test_format_facts_renders_key_value_lines | Format_facts emits one aligned ``key: value`` line per fact. SUT: doctor.format_facts. Asserts: assert len(lines) == 2; assert lines[0].startswith("os:"); assert lines[0].endswith("Linux") | unit | — |
| tests/test_doctor.py | 230 | test_smoke_torch_missing_is_ratcheted_diagnosis | Missing torch → exit 1, one stderr line that names the engine environment and carries the literal provisioning command. SUT: doctor.run_smoke. Asserts: assert code == 1; assert str(default_app_dir()) in line; assert "uv pip install torch --no-config --index" in line | unit | deliverable-fossils |
| tests/test_doctor.py | 243 | test_smoke_encoder_failure_names_error_and_next_action | An exploding encode (the wrong-wheel crash shape) → exit 1, one stderr line naming the failure plus the re-pick remedy. SUT: doctor.run_smoke. Asserts: assert code == 1; assert "no kernel image" in line; assert "sr-initialize" in line | unit | deliverable-fossils |
| tests/test_doctor.py | 257 | test_smoke_encoder_construction_failure_is_caught | An encoder factory that raises is compressed to the same one-line actionable failure. SUT: doctor.run_smoke. Asserts: assert code == 1; assert "model download failed" in line; assert "sr-initialize" in line | unit | deliverable-fossils |
| tests/test_doctor.py | 274 | test_smoke_wrong_width_vector_is_a_failure | A wrong-width vector is a broken setup, not success — exit 1 with the actionable re-pick line. SUT: doctor.run_smoke. Asserts: assert code == 1; assert str(EMBED_DIM) in line; assert "sr-initialize" in line | unit | deliverable-fossils |
| tests/test_doctor.py | 287 | test_smoke_happy_path_reports_version_cuda_and_ok | Torch + encoder healthy → exit 0; stdout carries the torch version, the CUDA availability, and an ok summary. SUT: doctor.run_smoke. Asserts: assert code == 0; assert "2.11.0" in out; assert "False" in out | unit | deliverable-fossils |
| tests/test_doctor.py | 301 | test_smoke_real_model_end_to_end | With real torch provisioned, the production BgeEncoder path encodes one string and exits 0 (machine-local; skipped on torch-free CI). SUT: doctor.run_smoke. Asserts: assert code == 0; assert "ok" in out | integration | deliverable-fossils, wrong-level? |
| tests/test_doctor_cli.py | 39 | test_probe_exits_zero_and_prints_fact_keys | ``doctor probe`` in a torch-free env exits 0 and reports the always-present fact keys. SUT: CLI subprocess. Asserts: assert result.returncode == 0, result.stderr; assert key in result.stdout, f"missing fact {key!r} in: {result.stdout}" | integration | deliverable-fossils, semantic-redundancy? |
| tests/test_doctor_cli.py | 56 | test_smoke_torch_free_env_fails_loudly_with_remedy | ``doctor smoke`` without torch exits 1 with the one-line ratcheted diagnosis carrying the literal provisioning command. SUT: CLI subprocess. Asserts: assert result.returncode == 1; assert len(lines) == 1, f"expected one stderr line, got: {result.stderr!r}"; assert "Traceback" not in result.stderr | integration | deliverable-fossils, semantic-redundancy? |
| tests/test_doctor_cli.py | 69 | test_help_documents_both_actions | ``doctor --help`` exits 0 and documents probe and smoke. SUT: CLI subprocess. Asserts: assert result.returncode == 0, result.stderr; assert "probe" in result.stdout; assert "smoke" in result.stdout | integration | deliverable-fossils |
| tests/test_embed.py | 46 | test_chunk_short_text_single_chunk | Text at or under the size threshold is returned as a single, verbatim chunk. SUT: embed.chunk_text. Asserts: assert chunks == [text] | unit | — |
| tests/test_embed.py | 53 | test_chunk_empty_is_no_chunks | Empty or whitespace-only text yields no chunks (caller skips embedding). SUT: embed.chunk_text. Asserts: assert embed.chunk_text("", size=1200, overlap=150) == []; assert embed.chunk_text(" \n\t ", size=1200, overlap=150) == []; assert embed.chunk_text(None, size=1200, overlap=150) == [] | unit | — |
| tests/test_embed.py | 60 | test_chunk_long_text_overlapping | Long text splits into overlapping windows with complete, ordered coverage. Each chunk stays within ``size`` chars; consecutive chunks advance by ``size - overlap`` and share an … SUT: embed.chunk_text. Asserts: assert len(chunks) > 1; assert all(len(c) <= size for c in chunks); assert embed.chunk_text(text, size=size, overlap=overlap) == chunks | unit | — |
| tests/test_embed.py | 89 | test_embed_chunks_one_vector_per_chunk | ``embed_chunks`` returns one ``EMBED_DIM`` vector per chunk, in order. SUT: embed.chunk_text, embed.embed_chunks, embed.EMBED_DIM. Asserts: assert len(expected_chunks) > 1 # guard: the fixture really is multi-chunk; assert len(vectors) == len(expected_chunks); assert all(len(v) == embed.EMBED_DIM for v in vectors) | unit | — |
| tests/test_embed.py | 103 | test_embed_chunks_empty_text_no_vectors | Whitespace-only text produces no vectors (nothing to embed). SUT: embed.embed_chunks. Asserts: assert embed.embed_chunks(" ", FakeEncoder()) == [] | unit | — |
| tests/test_embed.py | 108 | test_embed_pending_writes_per_chunk_rows | A single-chunk message writes 1 row; a multi-chunk message writes N rows. Rows carry ``owner_table='messages'``, ``owner_id=message_id``, ascending ``chunk_index`` 0..N-1, the c… SUT: embed.chunk_text, embed.embed_pending, embed.EMBED_MODEL, embed.EMBED_DIM. Asserts: assert written == 1 + n_long; assert short_rows == [(0, embed.EMBED_MODEL, embed.EMBED_DI… | unit | — |
| tests/test_embed.py | 142 | test_embed_pending_incremental_is_no_op_then_embeds_new | A second run with no new content writes nothing; a new message embeds only it. SUT: embed.embed_pending. Asserts: assert first == 1; assert again == 0; assert migrated_con.execute("SELECT count(*) FROM embeddings").fetchone()[0] == 1 | unit | — |
| tests/test_embed.py | 164 | test_embed_pending_skips_empty_text | Messages with NULL or whitespace-only text get no embedding row. SUT: embed.embed_pending. Asserts: assert written == 0; assert migrated_con.execute("SELECT count(*) FROM embeddings").fetchone()[0] == 0 | unit | — |
| tests/test_embed.py | 177 | test_embed_pending_is_model_aware | A vector under a different model is re-embedded under (and replaced by) the current model. The owner is selected (it lacks a *current-model* row), and because the ``embeddings``… SUT: embed.EMBED_DIM, embed.embed_pending, embed.EMBED_MODEL. Asserts: assert written == 1 # re-embedded under the current model; assert models == {embed.EMBED_MODEL} | unit | — |
| tests/test_embed.py | 209 | test_embed_pending_knn_nearest_chunk_is_expected | The cosine-nearest stored vector to a chunk's encoding is that chunk's row. SUT: embed.embed_pending. Asserts: assert nearest == target_id | unit | — |
| tests/test_embed.py | 244 | test_embed_cli_missing_warehouse_is_friendly | With no warehouse present, the CLI exits nonzero with a 'run ingest' hint (and never constructs the torch-backed encoder). The hint names the on-path command (`stockroom ingest`… SUT: embed.main. Asserts: assert code == 1; assert "run `stockroom ingest` first" in err | integration | — |
| tests/test_embed.py | 255 | test_embed_cli_embeds_pending_and_reports_count | The CLI embeds pending messages with the injected encoder and prints a count. SUT: embed.main, warehouse.open. Asserts: assert code == 0; assert "embedded 1" in capsys.readouterr().out; assert con.execute("SELECT count(*) FROM embeddings").fetchone()[0] == 1 | integration | — |
| tests/test_embed.py | 272 | test_embed_cli_full_reembeds | ``--full`` re-embeds already-embedded content (incremental would be a no-op). SUT: embed.main, warehouse.open. Asserts: assert incremental == 0; assert "embedded 0" in capsys.readouterr().out; assert full == 0 | integration | — |
| tests/test_embed.py | 292 | test_embed_pending_on_progress_reports_message_counts | ``on_progress`` receives a start line and ``i/N`` lines per selected message. SUT: embed.embed_pending. Asserts: assert written == 3; assert lines[0] == "embed: 3 messages"; assert lines[1:] == [ | unit | — |
| tests/test_embed.py | 310 | test_embed_cli_quiet_default_has_no_mid_run_progress | Without ``--verbose``, stdout is only the final embedded-count line. SUT: embed.main. Asserts: assert code == 0; assert len(lines) == 1; assert "embedded 2" in lines[0] | integration | — |
| tests/test_embed.py | 325 | test_embed_cli_verbose_prints_progress_and_count | ``--verbose`` emits progress lines and still prints the embedded count. SUT: embed.main. Asserts: assert code == 0; assert "embed: 2 messages" in out; assert "embed: 1/2 messages" in out | integration | — |
| tests/test_embed.py | 340 | test_embed_cli_verbose_missing_warehouse_still_friendly | ``--verbose`` does not change the missing-warehouse friendly error path. SUT: embed.main. Asserts: assert code == 1; assert "run `stockroom ingest` first" in err | integration | — |
| tests/test_embed.py | 350 | test_bge_encoder_encodes_to_384_on_cpu | ``BgeEncoder`` loads ``bge-small-en-v1.5`` (no ``trust_remote_code``) and encodes to 384-dim; a default chunk stays within the 512-token window. Torch-gated: skipped under torch… SUT: embed.BgeEncoder, embed.EMBED_DIM, embed.chunk_text. Asserts: assert len(vectors) == 1; assert len(vectors[0]) == embed.EMBED_DIM; assert encoder.model.max_seq_length <= 512 | integration | — |
| tests/test_engine_env.py | 53 | test_noop_when_inexact_check_passes | Usable env (check exit 0) → noop; heal sync never invoked. SUT: engine_env.ensure_engine_env. Asserts: assert report.action == "noop"; assert report.app_dir == Path(os.path.abspath(app_dir)); assert len(calls) == 1 | unit | deliverable-fossils |
| tests/test_engine_env.py | 71 | test_heals_with_inexact_sync_when_check_fails | Incomplete env → probe then heal; heal is frozen+inexact, no exact. SUT: engine_env.ensure_engine_env. Asserts: assert report.action == "synced"; assert len(calls) == 2; assert "--check" in calls[0] | unit | deliverable-fossils |
| tests/test_engine_env.py | 92 | test_heal_command_never_omits_inexact | Torch-safe — every uv sync argv from ensure includes --inexact. SUT: engine_env.ensure_engine_env. Asserts: assert all("--inexact" in c for c in cmds) | unit | deliverable-fossils |
| tests/test_engine_env.py | 104 | test_ensure_invokes_torch_ensure_after_deps | After deps probe, ensure_torch runs against app_dir. SUT: engine_env.ensure_engine_env. Asserts: assert len(_stub_torch_ensure) == 1; assert Path(os.path.abspath(_stub_torch_ensure[0])) == Path( | unit | deliverable-fossils |
| tests/test_engine_env.py | 119 | test_torch_install_marks_synced | Deps noop + torch installed → overall synced. SUT: engine_env.ensure_engine_env. Asserts: assert report.action == "synced" | unit | — |
| tests/test_engine_env.py | 138 | test_missing_pyproject_is_noop_without_uv | Edge: no pyproject → noop/failed soft; runner never called. SUT: engine_env.ensure_engine_env. Asserts: assert report.action in {"noop", "failed"}; assert calls == []; assert report.reason | unit | — |
| tests/test_engine_env.py | 154 | test_uv_failure_is_soft_failed | Edge: heal subprocess failure → action failed with reason, no raise. SUT: engine_env.ensure_engine_env. Asserts: assert report.action == "failed"; assert "boom" in report.reason or report.reason | unit | — |
| tests/test_engine_env.py | 167 | test_runner_timeout_is_soft_failed | Edge: TimeoutExpired → failed soft. SUT: engine_env.ensure_engine_env. Asserts: assert report.action == "failed"; assert report.reason | unit | — |
| tests/test_ingest_claude.py | 22 | test_parse_ts_z_suffix_is_naive_utc | Trailing ``Z`` stamps become naive UTC wall clock. SUT: claude._parse_ts. Asserts: assert claude._parse_ts("2026-07-10T03:22:00.000Z") == datetime( | unit | implementation-coupled |
| tests/test_ingest_claude.py | 29 | test_parse_ts_offset_converts_to_naive_utc | Offset-aware stamps convert to UTC before tzinfo is dropped. SUT: claude._parse_ts. Asserts: assert claude._parse_ts("2026-07-09T22:22:00-05:00") == datetime( | unit | implementation-coupled |
| tests/test_ingest_claude.py | 36 | test_parse_ts_rejects_non_strings | Non-string / empty values yield ``None``. SUT: claude._parse_ts. Asserts: assert claude._parse_ts(None) is None; assert claude._parse_ts("") is None; assert claude._parse_ts(123) is None | unit | implementation-coupled |
| tests/test_ingest_claude.py | 50 | test_simple_conversation_thinking_dropped_tool_use_kept | A simple conversation keeps user(str) + assistant turns, drops the ``thinking`` block (text only), and turns ``tool_use`` into a tool call with its native id preserved as proven… SUT: claude.parse_session. Asserts: assert session.harness == "claude"; assert session.session_id == "11111111-1111-4111-8111-111111111111"; assert [m.role for m in session.messa… | unit | — |
| tests/test_ingest_claude.py | 72 | test_per_message_model_and_token_columns | Each assistant turn carries its model and the four typed token counts. SUT: claude.parse_session. Asserts: assert a.model == "claude-sonnet-4-6"; assert a.input_tokens == 12; assert a.output_tokens == 85 | unit | — |
| tests/test_ingest_claude.py | 86 | test_tool_result_user_record_dropped_and_parent_resolves_past_it | An inputs-only ``tool_result`` user record produces no message; the next assistant's parent walks past it to the nearest kept ancestor. SUT: claude.parse_session. Asserts: assert last.text == "Added `hello()` to `src/utils.py`."; assert last.parent_ordinal == 1 # resolved past the dropped tool_result turn; assert last.tool_calls == [] | unit | — |
| tests/test_ingest_claude.py | 100 | test_user_content_list_shapes | A user turn whose content is a list of text blocks is kept (joined); a user turn whose content is only ``tool_result`` is dropped. SUT: claude.parse_session. Asserts: assert [m.role for m in session.messages] == ["user", "assistant", "assistant"]; assert user.text == "Here is the spec:\nmake it idempotent"; assert session.messages[2].parent_ordinal == 1 | unit | — |
| tests/test_ingest_claude.py | 115 | test_branching_parentuuid_uses_tree_not_position | Multi-model conversation: parents follow the ``parentUuid`` chain (here a clean linear chain) and each turn keeps its own model. SUT: claude.parse_session. Asserts: assert models == [None, "claude-sonnet-4-6", None, "claude-opus-4-6"]; assert [m.parent_ordinal for m in session.messages] == [None, 0, 1, 2] | unit | — |
| tests/test_ingest_claude.py | 125 | test_huge_tool_input_round_trips_untruncated | A large ``tool_input`` is captured whole (no truncation at rest). SUT: claude.parse_session. Asserts: assert tc.tool_name == "Write"; assert tc.tool_input == expected_input; assert tc.tool_input["contents"] == expected_input["contents"] | unit | — |
| tests/test_ingest_claude.py | 138 | test_title_agent_name_version_and_time_span | Metadata records fold into the session: ai-title -> title, version is captured, and started/ended are the min/max kept-message timestamps. SUT: claude.parse_session. Asserts: assert session.title == "Add hello function to utils"; assert session.agent_name is None; assert session.harness_version == "2.1.0" | unit | — |
| tests/test_ingest_claude.py | 153 | test_custom_title_and_agent_name_with_ignorable_types | The robustness fixture: ignorable record types produce no rows and never crash; ``custom-title`` and ``agent-name`` fold into the session. SUT: claude.parse_session. Asserts: assert [m.role for m in session.messages] == ["user", "assistant"]; assert [m.ordinal for m in session.messages] == [0, 1]; assert session.title == "Rename helper + callers" # custom… | unit | — |
| tests/test_ingest_claude.py | 165 | test_subagent_identity_and_spawn_link | A Claude subagent is its own session: id = file stem (NOT the parent's sessionId it carries), parent linkage + spawning_tool_use_id from meta.json, agent_name from its agent-nam… SUT: claude.parse_session, claude.parse_subagent. Asserts: assert sub.is_subagent is True; assert sub.session_id == "agent-aaa111"; assert sub.parent_session_id == "22222222-2222… | unit | — |
| tests/test_ingest_cli.py | 56 | test_cli_full_single_harness_writes_only_that_harness | ``--full --harness claude`` exits 0 and writes only Claude rows. SUT: ingest_cli. Asserts: assert result.returncode == 0, result.stderr; assert _warehouse_harnesses(home) == {"claude"} | integration | — |
| tests/test_ingest_cli.py | 77 | test_cli_full_both_harnesses_and_summary | A default ``--full`` run ingests both harnesses and prints a summary. SUT: ingest_cli. Asserts: assert result.returncode == 0, result.stderr; assert _warehouse_harnesses(home) == {"cursor", "claude"}; assert "cursor" in result.stdout | integration | — |
| tests/test_ingest_cli.py | 99 | test_cli_rejects_unknown_harness | An unknown ``--harness`` value is rejected by argparse (nonzero exit). SUT: ingest_cli. Asserts: assert result.returncode != 0 | integration | — |
| tests/test_ingest_cli.py | 118 | test_cli_quiet_default_has_no_mid_run_progress | Without ``--verbose``, stdout is only the end-of-run summary. SUT: ingest_cli. Asserts: assert result.returncode == 0, result.stderr; assert lines[0] == "ingest complete:"; assert any("claude:" in line for line in lines) | integration | — |
| tests/test_ingest_cli.py | 142 | test_cli_verbose_prints_progress_and_summary | ``--verbose`` emits harness progress lines and still prints the summary. SUT: ingest_cli. Asserts: assert result.returncode == 0, result.stderr; assert "claude: " in result.stdout; assert any( | integration | — |
| tests/test_ingest_cursor.py | 23 | test_simple_conversation_dense_ordinals_linear_parents | A simple conversation yields ordered messages with dense ordinals and a linear previous-kept parent chain (root parent is None). SUT: ingest_cursor. Asserts: assert session.harness == "cursor"; assert session.session_id == "simple-conversation"; assert session.source_path.endswith("simple-conversation.jsonl") | unit | — |
| tests/test_ingest_cursor.py | 41 | test_cursor_has_no_model_or_token_or_ts_grain | Cursor exposes no per-message model/tokens/timestamp — all honestly None. SUT: ingest_cursor. Asserts: assert m.model is None; assert m.ts is None; assert m.input_tokens is None | unit | — |
| tests/test_ingest_cursor.py | 58 | test_assistant_text_plus_tool_use_block_index_ordinal | An assistant turn keeps its text and emits a tool_call whose ordinal is the tool_use's block index; Cursor tool calls carry no source id. SUT: ingest_cursor. Asserts: assert turn.text == "I'll read the file first, then add the function."; assert len(turn.tool_calls) == 1; assert tc.tool_name == "Read" | unit | — |
| tests/test_ingest_cursor.py | 73 | test_empty_text_block_is_kept_not_dropped | A turn whose only text block is '' keeps text == '' (turn not dropped). SUT: ingest_cursor. Asserts: assert turn.text == ""; assert turn.tool_calls[0].tool_name == "StrReplace" | unit | — |
| tests/test_ingest_cursor.py | 81 | test_many_tools_distinct_block_index_ordinals | A single assistant turn with many tool_use blocks keeps empty text and assigns each tool call its distinct content-block-index ordinal. SUT: ingest_cursor. Asserts: assert tools_turn.text == ""; assert names == [ | unit | — |
| tests/test_ingest_cursor.py | 99 | test_turn_ended_error_is_consumed_not_stored | A ``turn_ended`` (even status:error) produces no row; prior turns remain. SUT: ingest_cursor. Asserts: assert [m.role for m in session.messages] == ["user", "assistant"]; assert [m.ordinal for m in session.messages] == [0, 1]; assert session.messages[1].tool_calls[0].tool_name == "Shell" | unit | — |
| tests/test_ingest_cursor.py | 107 | test_subagent_session_identity_and_parent_linkage | A Cursor subagent file becomes its own session: is_subagent, parent linkage to the conversation dir, agent_id = file stem, agent_type from the parent's Task tool_use input, and … SUT: ingest_cursor. Asserts: assert sub.is_subagent is True; assert sub.session_id == "00000000-0000-4000-8000-0000000000a1"; assert sub.agent_id == "00000000-0000-4000-8000-0000… | unit | — |
| tests/test_ingest_enrich.py | 19 | test_absent_db_returns_empty_no_error | A missing DB file yields empty enrichment rather than raising. SUT: ingest_enrich. Asserts: assert enrich.read_enrichment(tmp_path / "does-not-exist.db") == {} | unit | — |
| tests/test_ingest_enrich.py | 24 | test_none_path_returns_empty | A ``None`` path (no DB configured) yields empty enrichment. SUT: ingest_enrich. Asserts: assert enrich.read_enrichment(None) == {} | unit | — |
| tests/test_ingest_enrich.py | 29 | test_present_db_returns_models_keyed_by_conversation | A present DB returns each conversation's distinct models in row order. SUT: ingest_enrich. Asserts: assert result == { | unit | — |
| tests/test_ingest_enrich.py | 38 | test_reads_models_from_ai_code_hashes | Models are read from ``ai_code_hashes`` (current Cursor schema). SUT: ingest_enrich. Asserts: assert enrich.read_enrichment(db_path) == { | unit | — |
| tests/test_ingest_enrich.py | 73 | test_merges_conversation_summaries_models | ``conversation_summaries.model`` contributes models not already seen. SUT: ingest_enrich. Asserts: assert enrich.read_enrichment(db_path) == { | unit | — |
| tests/test_ingest_enrich.py | 117 | test_malformed_db_is_graceful | A DB without the expected tables no-ops (robust against schema drift). SUT: ingest_enrich. Asserts: assert enrich.read_enrichment(db_path) == {} | unit | — |
| tests/test_ingest_enrich.py | 127 | test_default_db_path_env_override | ``STOCKROOM_AI_TRACKING_DB`` overrides the default DB location. SUT: ingest_enrich. Asserts: assert enrich.default_db_path() == target | unit | — |
| tests/test_ingest_enrich.py | 136 | test_default_db_path_prefers_ai_tracking_subdir | Unconfigured resolution prefers ``~/.cursor/ai-tracking/...`` when present. SUT: ingest_enrich. Asserts: assert enrich.default_db_path() == modern | unit | — |
| tests/test_ingest_enrich.py | 153 | test_default_db_path_falls_back_to_legacy_location | When the modern path is missing, the legacy ``~/.cursor/...`` file wins. SUT: ingest_enrich. Asserts: assert enrich.default_db_path() == legacy | unit | — |
| tests/test_ingest_enrich.py | 167 | test_default_db_path_uses_wsl_windows_mount_fallback | WSL ``/mnt/<drive>/Users/.../ai-tracking/...`` candidates are searched. SUT: ingest_enrich. Asserts: assert enrich.default_db_path() == db_file | unit | — |
| tests/test_ingest_enrich.py | 183 | test_default_db_path_returns_modern_conventional_when_absent | With no candidates on disk, return the documented modern conventional path. SUT: ingest_enrich. Asserts: assert enrich.default_db_path() == ( | unit | — |
| tests/test_ingest_fixtures.py | 53 | test_harness_roots_exist | Both harness fixture roots are present. SUT: ingest_fixtures. Asserts: assert cursor_root.is_dir(); assert claude_root.is_dir() | unknown | — |
| tests/test_ingest_fixtures.py | 59 | test_cursor_session_files_present_and_valid | Each Cursor session file exists, parses, and sits at ``<conv>/<conv>.jsonl``. SUT: ingest_fixtures. Asserts: assert path.is_file(), f"missing cursor fixture: {path}"; assert _read_jsonl(path), f"empty cursor fixture: {path}"; assert path.stem == path.parent.name | unknown | — |
| tests/test_ingest_fixtures.py | 69 | test_claude_session_files_present_and_valid | Each Claude session file exists and parses as JSON-per-line. SUT: ingest_fixtures. Asserts: assert path.is_file(), f"missing claude fixture: {path}"; assert _read_jsonl(path), f"empty claude fixture: {path}" | unknown | — |
| tests/test_ingest_fixtures.py | 77 | test_cursor_subagent_nesting | The Cursor subagent child lives in ``<conv>/subagents/<child>.jsonl``. SUT: ingest_fixtures. Asserts: assert child.is_file(); assert _read_jsonl(child) | unknown | — |
| tests/test_ingest_fixtures.py | 87 | test_claude_subagent_nesting_and_meta | The Claude subagent sits in ``<session>/subagents/`` with a trimmed meta. The ``.meta.json`` carries exactly the real key set ``{agentType, description, toolUseId}`` — the m1 fi… SUT: ingest_fixtures. Asserts: assert child.is_file(); assert meta.is_file(); assert set(meta_obj) == {"agentType", "description", "toolUseId"} | unknown | — |
| tests/test_ingest_fixtures.py | 105 | test_robustness_fixture_covers_metadata_and_ignorable_types | The robustness fixture emits the metadata + ignorable record types. Real Claude logs interleave many record types the m1 fixtures omitted; the Claude parser must fold the metada… SUT: ingest_fixtures. Asserts: assert ignorable <= types, f"missing ignorable types: {ignorable - types}"; assert {"custom-title", "agent-name"} <= types; assert {"user", "assist… | unknown | — |
| tests/test_ingest_fixtures.py | 129 | test_ai_tracking_db_builds_with_expected_schema | The synthetic enrichment DB materializes with the current model tables. SUT: ingest_fixtures. Asserts: assert ai_tracking_db.is_file(); assert "ai_code_hashes" in tables; assert "conversation_summaries" in tables | unknown | — |
| tests/test_ingest_model.py | 20 | test_tool_call_minimal_and_defaults | A tool call needs only ordinal/name/input; provenance id defaults None. SUT: ingest_model. Asserts: assert tc.ordinal == 0; assert tc.tool_name == "Read"; assert tc.tool_input == {"path": "x"} | unit | — |
| tests/test_ingest_model.py | 29 | test_message_minimal_and_defaults | A message needs only ordinal/role; every other field defaults None/empty. SUT: ingest_model. Asserts: assert m.ordinal == 0; assert m.role == "user"; assert m.parent_ordinal is None | unit | — |
| tests/test_ingest_model.py | 46 | test_message_tool_calls_are_independent_per_instance | The default ``tool_calls`` list is not shared between instances. SUT: ingest_model. Asserts: assert a.tool_calls != b.tool_calls; assert b.tool_calls == [] | unit | — |
| tests/test_ingest_model.py | 55 | test_session_minimal_and_defaults | A session needs harness/session_id/source_path; the rest default to honest absence (None / empty list), never fabricated values. SUT: ingest_model. Asserts: assert s.harness == "cursor"; assert s.session_id == "conv-1"; assert s.source_path == "/p/conv-1.jsonl" | unit | — |
| tests/test_ingest_model.py | 85 | test_value_equality_across_instances | Dataclasses compare by value, so equal content compares equal. SUT: ingest_model. Asserts: assert mk() == mk() | unit | — |
| tests/test_ingest_model.py | 112 | test_session_messages_are_independent_per_instance | The default ``messages`` list is not shared between session instances. SUT: ingest_model. Asserts: assert b.messages == [] | unit | — |
| tests/test_ingest_orchestrator.py | 53 | test_full_ingest_summary_matches_table_counts | A full fixture ingest populates all tables and the summary counts equal the actual table row counts. SUT: ingest.ingest. Asserts: assert summary.sessions == _count(migrated_con, "sessions"); assert summary.messages == _count(migrated_con, "messages"); assert summary.tool_calls == _count(migrated_con, "tool_calls") | integration | — |
| tests/test_ingest_orchestrator.py | 70 | test_harness_filter_writes_only_selected | ``harness='claude'`` ingests only Claude rows. SUT: ingest.ingest. Asserts: assert harnesses == {"claude"} | integration | — |
| tests/test_ingest_orchestrator.py | 88 | test_second_incremental_ingest_is_noop | After a full ingest, an incremental run writes no new rows (watermark). SUT: ingest.ingest. Asserts: assert after == before; assert summary.sessions == 0 | integration | — |
| tests/test_ingest_orchestrator.py | 104 | test_on_progress_none_emits_nothing | Default ``on_progress=None`` does not require a callback and completes. SUT: ingest.ingest. Asserts: assert summary.by_harness["claude"].sessions > 0 | integration | naming-lies |
| tests/test_ingest_orchestrator.py | 116 | test_on_progress_reports_harness_start_and_session_counts | ``on_progress`` receives a harness-start line and ``i/N`` lines per selected conversation. SUT: ingest.ingest. Asserts: assert lines, "expected progress lines"; assert start.startswith("claude: "); assert start.endswith(" sessions") | integration | — |
| tests/test_ingest_orchestrator.py | 142 | test_subagents_link_to_parents_across_the_run | Every subagent session resolves to an existing parent session row, and each Claude subagent's spawn id joins a parent tool-call provenance id. SUT: ingest.ingest. Asserts: assert orphans == []; assert len(joined) == 1 | integration | — |
| tests/test_ingest_orchestrator.py | 171 | test_inputs_only_no_tool_result_text_stored | No dropped tool_result output text leaks into messages (inputs only). SUT: ingest.ingest. Asserts: assert leaked == 0 | integration | — |
| tests/test_ingest_orchestrator.py | 184 | test_ordinals_are_dense_per_session | Each session's message ordinals are a dense 0..n-1 run. SUT: ingest.ingest. Asserts: assert ordinals == list(range(len(ordinals))) | integration | — |
| tests/test_ingest_orchestrator.py | 199 | test_cursor_enrichment_applied_to_models | The synthetic ai-code-tracking models land on the matching Cursor session. SUT: ingest.ingest. Asserts: assert models == ["gpt-5", "claude-4.6-sonnet"] | integration | — |
| tests/test_ingest_orchestrator.py | 216 | test_project_id_is_the_verbatim_slug | Each session's ``project_id`` is the project dir's verbatim slug — the grouping identity, stored exactly as the harness encodes it (Cursor drops the leading separator; Claude ke… SUT: ingest.ingest. Asserts: assert cursor_pid == "home-user-project"; assert claude_pid == "-home-user-project" | integration | — |
| tests/test_ingest_orchestrator.py | 238 | test_cursor_cwd_recovered_from_inband_path | A Cursor conversation with an in-band absolute path recovers the real ``cwd`` by re-encode-and-match — including a hyphenated leaf the naive dash->slash decode would have mangle… SUT: ingest.ingest. Asserts: assert cwd == "/home/user/lite-rpg" | integration | — |
| tests/test_ingest_orchestrator.py | 256 | test_cursor_cwd_is_null_without_evidence | A Cursor conversation with no in-band path that re-encodes to its slug gets an honest ``cwd = NULL`` (never a fabricated decode). SUT: ingest.ingest. Asserts: assert cwd is None | integration | — |
| tests/test_ingest_orchestrator.py | 272 | test_no_fabrication_roundtrip_invariant | Corpus-wide: a populated ``cwd`` always re-encodes back to its own ``project_id``. This is the core correctness claim — a fabricated path is structurally impossible to store, si… SUT: ingest.ingest. Asserts: assert rows # the corpus is non-empty; assert encode_for(harness, cwd) == project_id | integration | — |
| tests/test_ingest_orchestrator.py | 292 | test_full_ingest_persists_source_mtime_for_sessions_and_subagents | Each session uses its discovered conversation mtime; subagents inherit it. SUT: ingest.ingest. Asserts: assert rows; assert source_mtime is not None; assert source_mtime == by_identity[(harness, parent_id)] | integration | — |
| tests/test_ingest_orchestrator.py | 436 | test_ingest_output_matches_golden_snapshot | A full fixture ingest byte-matches the committed golden reconstruction. This locks every ordinal, parent_id, drop, token value, model, subagent edge, and the ``project_id``/``cw… SUT: ingest.ingest. Asserts: assert GOLDEN_PATH.is_file(), f"golden ingest snapshot missing: {GOLDEN_PATH}"; assert actual == expected | integration | wrong-level? |
| tests/test_ingest_paths.py | 16 | test_encode_collapses_every_nonalnum_to_dash | Every non-alphanumeric char (``/``, ``.``, ``_``, ``-``) becomes ``-``. SUT: ingest_paths. Asserts: assert paths.encode("/home/user/lite-rpg") == "-home-user-lite-rpg"; assert (; assert paths.encode("/home/user/a_b") == "-home-user-a-b" | unit | — |
| tests/test_ingest_paths.py | 25 | test_encode_for_cursor_strips_leading_separator | Cursor's slug drops the leading path separator (slugs start with a word). SUT: ingest_paths. Asserts: assert paths.encode_for("cursor", "/home/user/lite-rpg") == "home-user-lite-rpg" | unit | — |
| tests/test_ingest_paths.py | 30 | test_encode_for_claude_keeps_leading_separator | Claude's slug keeps the leading separator as a leading ``-``. SUT: ingest_paths. Asserts: assert paths.encode_for("claude", "/home/user/project") == "-home-user-project" | unit | — |
| tests/test_ingest_paths.py | 35 | test_resolve_cursor_recovers_hyphen_leaf | A hyphenated leaf is recovered from an in-band path (naive decode breaks). SUT: ingest_paths. Asserts: assert paths.resolve_cwd("cursor", slug, texts=texts) == "/home/user/lite-rpg" | unit | — |
| tests/test_ingest_paths.py | 42 | test_resolve_cursor_recovers_dot_leaf | A dotted leaf is recovered too (``.`` also collapses to ``-`` on encode). SUT: ingest_paths. Asserts: assert ( | unit | — |
| tests/test_ingest_paths.py | 51 | test_resolve_cursor_walks_ancestors_to_project_root | A deeper in-band path resolves to the ancestor whose encode == slug. SUT: ingest_paths. Asserts: assert paths.resolve_cwd("cursor", slug, texts=texts) == "/home/user/lite-rpg" | unit | — |
| tests/test_ingest_paths.py | 58 | test_resolve_cursor_null_when_no_inband_path | No in-band absolute path -> honest ``None`` (never a fabricated guess). SUT: ingest_paths. Asserts: assert paths.resolve_cwd("cursor", slug, texts=texts) is None | unit | — |
| tests/test_ingest_paths.py | 65 | test_resolve_claude_record_cwd_short_circuits | Claude's authoritative record ``cwd`` is returned directly. SUT: ingest_paths. Asserts: assert ( | unit | — |
| tests/test_ingest_paths.py | 75 | test_resolve_is_deletion_proof_no_filesystem_access | Recovery works purely from stored text, even for a path absent on disk. This is the survive-deletion invariant: ``cwd`` is recoverable from the transcript after the project dire… SUT: ingest_paths. Asserts: assert paths.resolve_cwd("cursor", slug, texts=texts) == "/home/user/deleted-proj" | unit | — |
