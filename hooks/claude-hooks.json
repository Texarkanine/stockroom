{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "export PATH=\"$HOME/.local/bin:/usr/bin:/bin:$PATH\"; { PYTHONPATH=\"${CLAUDE_PLUGIN_ROOT}/skills/sr-search/src\" python3 -m stockroom shim rectify --owner claude --app-dir \"${CLAUDE_PLUGIN_ROOT}/skills/sr-search\"; stockroom dashboard; } >/dev/null 2>&1 || true",
            "timeout": 300
          }
        ]
      }
    ]
  }
}
