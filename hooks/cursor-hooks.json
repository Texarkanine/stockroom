{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "type": "command",
            "command": "PYTHONPATH=\"${CURSOR_PLUGIN_ROOT}/skills/sr-search/src\" uv run --project \"${CURSOR_PLUGIN_ROOT}/skills/sr-search\" --no-sync --no-config python -m stockroom shim rectify --owner cursor --app-dir \"${CURSOR_PLUGIN_ROOT}/skills/sr-search\" >/dev/null 2>&1 || true",
            "timeout": 10000
          }
        ]
      }
    ]
  }
}
